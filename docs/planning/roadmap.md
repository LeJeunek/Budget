# FinanceOS — Roadmap

Phases are sequential. Each phase must be feature-complete, tested, reviewed, and approved (per Release Manager checklist) before the next phase's features are architected. A phase is not "AI features" or "polish" scattered throughout — it is a vertical slice a user could actually use.

---

## Phase 0 — Foundation
**Goal:** empty repo → deployable skeleton with auth and a database, nothing else.

- Next.js 15 App Router project scaffold, TypeScript, Tailwind, shadcn/ui installed
- PostgreSQL provisioned (local dev via Docker; hosted for staging)
- Prisma initialized, connected, `User` model only
- Better Auth wired up: email login + Google OAuth
- Base app shell: sidebar layout, top nav, dark/light theme toggle, responsive breakpoints (no feature content yet)
- CI-equivalent local checks: typecheck, lint, test runner configured
- Deployment target decided and a "hello world" deploy verified

**Explicitly excluded:** any financial data model, any dashboard content.

**Owners:** CTO (this doc) → Solution Architect (project skeleton + folder structure) → Database Architect (User model only) → Backend Engineer (auth wiring) → Frontend Lead (shell layout).

**Status: done.**

---

## Phase 1 — Core MVP: Accounts, Transactions, Basic Dashboard
**Goal:** a user can add accounts, enter/import transactions, and see a real dashboard built from their own data. This is the load-bearing phase — the data model here (accounts, transactions, categories) underpins every later phase.

In scope:
- **Accounts**: create/edit/delete accounts of every type (checking, savings, credit card, cash, investment, retirement, crypto); balance, institution, interest rate, color
- **Transactions**: table with date/merchant/category/amount/account/notes/tags; search, sort, pagination, filters; manual add/edit/delete; CSV import; split transactions; notes.
- **Categories**: the fixed starter category list seeded automatically per user and protected from rename/delete; users can add/rename/recolor/delete their own custom categories on top of that set.
- **Dashboard Overview v1**: Net Worth, Monthly Income, Monthly Expenses, Cash Flow, Savings Rate; charts: Spending by Category, Income vs Expense, Monthly Trends
- **Global search v1**: transactions + accounts only
- Loading skeletons, toast notifications, basic responsive polish

**Hand-off:** Solution Architect designs Phase 1 folder structure, module boundaries, and API contracts next. Database Architect then extends the schema with Account, Transaction, Category, Tag models.

**Status: done.**

---

## Phase 2 — Budgeting, Goals, Bills
**Goal:** a user can plan a month, save toward something, and never miss a bill.

In scope:
- **Budgeting**: monthly planner per category, allocated/spent/remaining, progress bar, percentage used, over-budget indicator; Dashboard's "Remaining Budget" and "Budget Health Score" go live
- **Savings Goals**: goal CRUD, current progress, estimated completion, monthly contributions, remaining amount, progress visualization
- **Bills**: recurring bill CRUD, due dates, recurring schedule, paid/late status, upcoming list
- **Calendar v1**: bills + paydays only (paydays deferred in practice — see Phase 2's product spec — because Recurring Income didn't exist as a domain until Phase 3a; Calendar v1 shipped bills-only)
- **Notifications v1**: budget exceeded, bill due (in-app only)
- Receipt attachment, with file storage (UploadThing) wired up

**Hand-off:** Solution Architect + Database Architect extend schema with Budget, BudgetCategory, Goal, Bill, Receipt models before implementation starts.

**Status: done.**

---

## Phase 3 — Debt, Investments, Recurring Income, Analytics

### CTO decision (2026-07-20): split into Phase 3a and Phase 3b

Phase 3 as originally scoped bundles four data-owning domains (Debt, Investments, Recurring Income, broad Financial Goals) with a 12-metric Analytics suite in a single review cycle. Splitting it is the right call, for three concrete reasons rather than just "it's big":

1. **Real dependency chain, not just size.** Analytics' most meaningful metrics (net worth growth/history, income growth, income sources, subscription cost detection) and the Dashboard's Net Worth History chart are only meaningful once Debt, Investments, and Recurring Income data exists. Architecting Analytics before that data model is settled risks designing against a moving target — the same class of risk the schema-narrowness concern in Risk #1 already warns about. Building Analytics second, on a stable 3a data model, is lower-risk than building it concurrently.
2. **Review-cycle size.** Phase 2 already showed that three parallel independent domains (Budgeting/Goals/Bills) plus one dependent one (Notifications) was near the practical ceiling for a single clean review cycle before things start slipping (session-limit interruptions, doc-conflict discovery mid-implementation). Phase 3 as originally scoped is four independent domains *plus* a 12-item analytics suite — meaningfully larger than Phase 2. Splitting keeps each sub-phase's Product Owner → Architect → Database Architect → Backend → Frontend → Review pipeline at a size this team has already proven it can execute cleanly.
3. **Charter's "demoable increment" principle.** The Charter requires each phase produce a usable, demoable increment, not just a checkpoint. "Debt + Investments + Recurring Income" is itself a complete, demoable increment (a user's full net worth and income picture becomes accurate). "Analytics + broad Financial Goals + Net Worth History" is a distinct, later increment (insight and analysis *on top of* data that already exists). Forcing both into one un-splittable phase would mean nothing ships until both are done.

The split follows the natural data-dependency line: **3a produces the data, 3b produces the insight.** Financial Goals (broad) is placed in 3b rather than 3a because one of its three goal types — "pay off debt" — is blocked on 3a's Debt Tracker; it's also conceptually closer to the tracking/completion layer 3b represents than to the raw data-entry layer 3a represents.

The 3a/3b boundary is binding, per Risk #6/#13 — no feature moves across it without CTO approval.

---

## Phase 3a — Debt, Investments, Recurring Income
**Goal:** the user's net worth and income picture becomes complete and accurate — what's owed and what's invested are no longer missing from the dashboard.

In scope:
- **Debt Tracker**: credit cards/loans/student loans/mortgage; balance, interest rate, minimum payment, payoff date, total interest remaining, snowball vs avalanche comparison
- **Investments**: portfolio overview (stocks, ETFs, 401k, IRA, crypto), current value, gain/loss, allocation, historical growth, sector allocation, dividend income (manual entry, not live market data feeds — that remains a post-v1 decision)
- **Recurring income tracking**: salary, side hustles, dividends, rental income, bonuses
- **Net Worth aggregation update**: extend the existing Phase 1 `getNetWorth` dashboard service to include Debt liabilities and Investment values (the chart itself stays off until 3b — see below)
- **Net Worth Snapshot capture (backend only, no UI)**: begin periodically recording net worth (and its Debt/Investment/Account components) as soon as this data exists. This does **not** wait for 3b's Net Worth History chart — if snapshotting only starts when the chart is built, the chart launches with an empty history and is useless for months. See Risk #10.

**Open schema question — for the Database Architect, not decided here:** the `Account` model already has a `type` discriminator covering `CREDIT_CARD`/`INVESTMENT`/`RETIREMENT`/`CRYPTO`. The Database Architect must decide and document whether Debt and Investments are:
(a) new models (`Debt`, `Investment`/`Holding`) that reference an existing `Account` of the matching type, or
(b) standalone models independent of `Account`, or
(c) some hybrid (e.g., an `Account` remains the ledger-balance record, a linked `Debt`/`Investment` record holds the type-specific fields — interest rate, payoff schedule, holdings, allocation — that don't belong on the generic `Account` model).
This decision directly affects how straightforward the Net Worth aggregation update and 3b's Analytics queries will be — resolve it once, explicitly, before backend implementation starts (Risk #9).

**Milestones (build order and why):**
1. **Product Owner specs** for Debt Tracker, Investments, Recurring Income — independent domains, can be dispatched in parallel (same pattern as Phase 2's Budgeting/Goals/Bills).
2. **CTO resolution pass** for any cross-doc conflicts the specs surface (established pattern from every prior phase — resolve directly rather than letting an implementing agent guess).
3. **Solution Architect + Database Architect**, one combined pass covering all three domains — this is where the Account-linkage question above gets decided and documented in `docs/database/`.
4. **Backend implementation, front-loaded by complexity:** Investments first, as its own dedicated pass (it is the most complex piece — valuation, allocation, gain/loss, historical growth, dividend income — surfacing problems here first while there's still schedule slack). Then Debt Tracker and Recurring Income in parallel (both independent of Investments and of each other).
5. **Net Worth aggregation update** (extends Phase 1's dashboard service) — depends on both Debt and Investments being complete.
6. **Net Worth Snapshot job** goes live — depends on step 5.
7. **Frontend** for Debt, Investments, Recurring Income (Frontend Lead, same domain-component pattern established in Phase 1/2).
8. **Full 3a review gate**: Security Architect (financial data sensitivity — same standing concern as Risk #4, now covering debt/investment balances), Performance Engineer (portfolio/payoff calculations), Bug Hunter, full live end-to-end browser verification against real data, Release Manager sign-off. **3b's architecture does not start until this gate is passed**, per this roadmap's own phase-gate rule.

**Status: done.**

---

## Phase 3b — Analytics, Net Worth History, Financial Goals
**Goal:** turn the now-complete data picture into insight — this is what makes the dashboard genuinely useful, and it closes out v1.

In scope:
- **Net Worth History chart** on the Dashboard goes live (snapshot data has been accumulating since the end of 3a)
- **Analytics** (full suite): yearly spending, category trends, income growth, savings growth, net worth growth/history, daily spending heatmap, expense distribution, budget vs actual, top merchants, largest purchases, income sources, subscription cost detection
- **Financial Goals (broad)**: pay off debt / save $X / increase savings rate — completion tracking, distinct from Savings Goals' single-target model. Product Owner must explicitly define the boundary between this and the existing Savings Goal model before schema work begins (Risk #12).

**Milestones (build order and why):**
1. **Product Owner spec pass**, grounded in 3a's now-live schema, covering Net Worth History, the full Analytics list, and Financial Goals (broad) — including the explicit SavingsGoal-vs-FinancialGoal boundary call-out above.
2. **Solution Architect + Database Architect**: Analytics is primarily a read/query layer over existing data (Transaction, Category, Budget, plus 3a's Debt/Investment/Recurring Income and the new Net Worth Snapshot) — the Architect must decide whether raw aggregation queries are sufficient or whether materialized/cached aggregates are needed given Risk #11. Schema work for the new `FinancialGoal` model happens here too.
3. **Build order:**
   a. **Net Worth History chart** first — smallest scope, highest visual payoff, and its data has already been accumulating since 3a shipped.
   b. **Analytics suite**, split into two backend passes to keep review-cycle size manageable (mirroring the reasoning behind the 3a/3b split itself): **Pass 1** — metrics derivable from data that has existed since Phase 1/2 (yearly spending, category trends, expense distribution, budget vs actual, top merchants, largest purchases, daily spending heatmap). **Pass 2** — metrics that depend on 3a's new models (income growth, income sources, savings growth, subscription cost detection).
   c. **Financial Goals (broad)** last — the smallest of the three 3b components, and its debt-payoff goal type depends on 3a's Debt Tracker, which is already live by this point.
4. **Full 3b review gate**: Security Architect, Performance Engineer (analytics query load, per Risk #11), Bug Hunter, full live end-to-end browser verification, Release Manager sign-off.

**v1 ships at the end of Phase 3b**, per the Charter's success definition (see the 2026-07-20 correction note in `project-charter.md`).

**Status: done. v1 shipped — see `docs/release/v0.1.0-notes.md` and `v0.1.0-checklist.md`.**

---

## Phase 4 — AI Features, Reports, Notifications v2, Customization, Admin

### CTO decision (2026-07-21): split into Phase 4a, 4b, 4c

Phase 4 as originally scoped bundles six technically distinct domains — AI/LLM integration, PDF generation, notification infrastructure + email delivery, calendar UI, theming/customization, and admin tooling — into one review cycle. This is a materially different, more *heterogeneous* mix than Phase 3's four data-domains-plus-analytics: Phase 3's pieces were all variations on "model and surface financial data," whereas Phase 4's pieces don't share a common technical shape at all. Splitting is the right call, for three concrete reasons:

1. **Risk-profile isolation, not just size.** AI is the one domain in this phase with a standing, explicit, per-feature Security Architect review requirement (Risk #2 — prompt injection via user-controlled transaction/merchant text) and it is also the *first LLM integration this codebase has ever had* — there is no existing pattern for prompt construction, structured-output validation, or third-party-model data handling to build on. Bundling it into the same review cycle as, say, accent-color theming would either dilute the scrutiny the AI work specifically needs, or force low-risk cosmetic work to wait behind a slower, higher-scrutiny review pipeline it doesn't need. Isolating it into its own sub-phase (4a) lets the AI-specific review pattern (design-stage security pass *and* pre-release security pass — see 4a milestones) get the dedicated attention Risk #2 calls for, without holding up the rest of Phase 4.
2. **A second, distinct risk-profile grouping: new external data-egress surfaces.** Reports (PDF generation) and Notifications v2 (email delivery) are grouped together (4b) because both are, for the first time in this codebase, features that take a user's financial data *outside the app boundary* — a PDF file a user downloads and may forward, an email sent through a third-party transactional-email provider. Neither depends on the AI work in 4a (a monthly PDF report and a low-balance email are just aggregation + rendering/delivery, not intelligence), but both require a net-new external service dependency (a PDF renderer, an email provider) that doesn't exist in this codebase yet, and both deserve their own Security Architect review pass focused on that egress surface (unauthorized report generation for another user's data, email content/PII exposure) — a materially different review focus than AI's prompt-injection concern. Grouping these two together, and only these two, keeps that review focused.
3. **Dependency-order and review-cycle-size argument for the third group.** Calendar v2, Customization, and Admin (4c) share no new external dependency and no elevated security posture on their own *except* Admin, which introduces this codebase's first-ever privilege tier (no `role`/admin concept exists on `User` today) and therefore its first authorization-bypass risk surface beyond "wrong user, right role." Admin is grouped with the two lower-risk domains rather than isolated further because (a) three sub-phases is already a meaningful increase in process overhead for one phase and a fourth would push past what Phase 2/3's proven cadence supports, and (b) Admin's audit-log feature is materially more useful, and more demoable, built *after* 4a and 4b ship — there is little to audit in an app with no AI decisions, no report downloads, and no email sends yet. Sequencing Admin last, as part of 4c, means its audit log launches with real events to show instead of an empty log — the same "don't build the empty-history feature first" reasoning Risk #10 already established for the Net Worth Snapshot job.

The 4a → 4b → 4c boundaries are binding, same as 3a/3b (Risk #6/#13, extended to Phase 4 below as Risk #16): no feature moves across a boundary without CTO approval. Each gate is sequential — 4b's architecture does not start until 4a's review gate passes, and 4c's does not start until 4b's does — consistent with this roadmap's standing phase-gate rule and with how 3a/3b was actually run.

**Two stale cross-doc scope items resolved here, before Product Owner work starts** (same pattern as the AI-insight/v1-bar correction in `project-charter.md` and the SavingsGoal/FinancialGoal boundary in 3b):

- **"Custom categories" is removed from 4c's User Customization scope.** Phase 1 already shipped full per-user custom category CRUD (add/rename/recolor/delete on top of the protected starter set) — see `docs/product/categories.md`, confirmed live in `src/features/categories/`. The original Phase 4 bullet list predates that spec's resolution and is now a stale duplicate. Nothing is lost: if a genuinely new categories capability surfaces later (e.g., category icons, nesting/grouping), it gets its own spec at that time, not folded silently into "customization."
- **Admin's "manage categories" is a distinct feature from the above, and must be spec'd as such.** Read literally, it plausibly means an admin-level capability to edit the *global starter-category template* every new user is seeded with (the 11 system categories), not a user's own custom categories. Product Owner must state this explicitly in the Admin spec rather than leave it ambiguous, since the two are easy to conflate given the naming collision.
- **Calendar v2 should explicitly pick up paydays**, not just "recurring transactions, budget reset" as currently worded. Calendar v1's product spec explicitly deferred paydays because Recurring Income didn't exist yet as a domain ("deferred to whenever Phase 3's recurring income feature ships and can be layered onto this same calendar view at that point" — `docs/product/calendar-and-notifications.md`). Recurring Income shipped in 3a. Product Owner should treat paydays as in-scope for Calendar v2 even though the current roadmap bullet doesn't name it, so this deferred item isn't silently dropped.

---

## Phase 4a — AI Features
**Goal:** the dashboard gets its first genuinely "smart" layer — a standalone, demoable increment on its own (auto-categorized transactions, a budget advisor, monthly summaries, spending insights, a financial health score) that does not depend on anything else in Phase 4.

In scope:
- **Transaction auto-categorization**: suggest a category for uncategorized (or newly imported) transactions, with user review/override — never silently overwrite a user's own categorization
- **AI budget advisor**: recommendations against the existing Budgeting data (Phase 2)
- **Automatic monthly summaries**: natural-language recap of the month's financial activity
- **Spending insights**: pattern-level observations surfaced to the user (e.g. unusual spending, trend call-outs)
- **Financial Health Score (0–100)**: see the open scope question below before this is spec'd

**Cross-cutting technical decision flagged, not made here:** which LLM/AI provider and integration approach to use. This codebase has **no existing AI/LLM integration of any kind** — this is a foundational choice, not an implementation detail, and it is the Solution Architect's (working jointly with the AI Engineer, per that role's charter) to make as part of 4a's architecture pass, not the CTO's to pick unilaterally. The CTO is, however, setting binding constraints on that choice, consistent with "approve architecture" and "review scalability":
  - Structured/validated output only — every model response must be parseable against a Zod schema before it touches the database or is trusted by any other service (already required by Risk #2's mitigation; restated here because it constrains provider choice, e.g. providers with reliable JSON/tool-call modes are strongly preferred over freeform text parsing).
  - Minimize what's sent to the third-party model — transaction/merchant text is the minimum necessary for categorization/insights; no sending full account numbers, auth tokens, or unrelated users' data. This is a data-privacy constraint on the integration design, not just a security-review checklist item.
  - The integration must be swappable — isolate provider-specific calls behind a single module boundary in `lib/ai/` (per the AI Engineer's existing role charter) so a future provider change is a contained swap, not a rewrite. Avoids vendor lock-in becoming a long-term maintainability problem.
  - Cost/latency must be bounded and observable — no unbounded per-request fan-out (e.g. one model call per transaction on every page load); caching/batching strategy is an architecture-pass decision, not an afterthought.

**Open scope question — for the Product Owner to resolve in the spec, not decided here:** is the Financial Health Score an LLM-computed judgment, or a deterministic formula over existing Analytics/Debt/Budget data (debt-to-income, savings rate, budget adherence, net worth trend) with an optional AI-generated narrative layered on top? This materially changes module ownership — a deterministic score is primarily Backend Engineer's extension of existing Analytics services with AI Engineer only adding narration text, whereas an LLM-computed score puts the AI Engineer in the critical path for a number users will treat as authoritative. Resolve this explicitly before the Solution Architect pass, the same way Risk #12 required an explicit boundary call before 3b's `FinancialGoal` schema work began.

**Milestones (build order and why):**
1. **Product Owner spec pass** for all five AI features. Unlike 3a's three fully independent domains, these five share one technical foundation and one review theme (they are not independently dispatchable in the same sense) — spec them as one cohesive "AI Features" document set, but resolve the Financial Health Score scope question above as part of this pass, before architecture.
2. **Solution Architect + AI Engineer joint architecture pass**: resolve the LLM provider/approach question within the constraints above; define `lib/ai/` module boundaries, prompt structure, and Zod structured-output schemas; define rate-limiting/cost-control strategy; define fallback behavior when the model is unavailable or returns invalid output (the product must degrade gracefully, not break). Database Architect determines whether new tables are needed (e.g. a suggestion/audit trail for auto-categorization, a historical `FinancialHealthScore` snapshot table analogous to 3a's Net Worth Snapshot).
3. **Design-stage Security Architect review**, before backend implementation starts — a refinement of Risk #2's mitigation specifically for this sub-phase: because this is the first AI feature ever built here, review the prompt-injection defense and structured-output validation plan at the design stage, not only at the final pre-release gate, to avoid costly rework if the defense pattern needs to change.
4. **Backend implementation, foundation-first:** transaction auto-categorization first — it establishes the reusable prompt/structured-output/Zod-validation pattern the other four features build on. The remaining four follow in whatever dependency order the architecture pass determines (e.g. if the Health Score consumes Spending Insights or Budget Advisor output, it is built last).
5. **Frontend** for all five surfaces (Frontend Lead + UI Component Engineer): categorization review/override UI, budget advisor card, monthly summary view, insights widget, health score display.
6. **Full 4a review gate**: Security Architect (final pre-release review per Risk #2 — prompt injection, structured-output validation, no unnecessary data sent to the model), Performance Engineer (LLM latency/cost, caching), Bug Hunter, full live end-to-end browser verification, Release Manager sign-off. **4b's architecture does not start until this gate passes.**

**Status: done. Review gate passed — Security Architect, Performance Engineer, and Bug Hunter all APPROVE; Release Manager APPROVE — see `docs/release/phase-4a-notes.md`.**

---

## Phase 4b — Reports & Notifications v2
**Goal:** a user's financial data can leave the app in controlled, useful ways for the first time — a downloadable PDF report, and notifications that reach them by email, not just in-app.

### CTO kickoff pass (2026-07-27): scope confirmed, reuse/integration boundaries set

Same discipline as every prior phase kickoff (3a's Account-linkage flag, 3b's stale-scope resolution, 4a's Financial Health Score scope resolution, this Phase 4 section's own split decision) — before the Product Owner spec pass begins, confirm this phase's scope as already written is still correct now that 4a is actually built, and resolve anything 4a's real shape changes about how 4b should be architected. 4a's review gate has passed (Security Architect, Performance Engineer, and Bug Hunter all APPROVE; Release Manager APPROVE — `docs/release/phase-4a-notes.md`), so per this roadmap's own phase-gate rule, 4b's architecture may now begin — starting, as every prior sub-phase has, with this scope/sequencing pass rather than jumping straight to Product Owner.

**Scope as originally written below is confirmed, with one ambiguity flagged for the Product Owner spec pass, not resolved here:** "weekly/monthly summary" is listed as a single Notifications v2 trigger type, but these are two different things and only one has a real data source today. A **monthly** summary-ready notification is a thin notify-and-link wrapper around 4a's already-existing `MonthlySummary` cron job (`app/api/cron/monthly-summary/route.ts`) — notify the user once that month's row is generated, linking to the already-built Dashboard card/history view; no new computation required. A **weekly** rollup has no equivalent anywhere in this codebase — nothing shipped in Phase 1 through 4a computes a weekly aggregate of anything. If "weekly summary" stays in scope, it is a **new aggregation feature** hiding inside what otherwise reads as "just wire up a notification," and the Product Owner spec must say so explicitly (what it contains, whether it's AI-narrated like its monthly sibling or a plain numeric digest, whether it needs its own persisted row) rather than have an implementing agent discover mid-build that a phase framed as "aggregation + rendering/delivery, not intelligence" quietly requires a net-new data-computation feature. Dropping "weekly" and keeping only "monthly" is a legitimate call for the Product Owner to make in the spec — not one made here.

**Two genuine integration opportunities 4a's actual shape surfaces, resolved here as binding constraints rather than left for an implementing agent to guess at — the same discipline behind this product's existing reuse precedents (Financial Health Score reusing the Budget Health Score verbatim; Monthly Summaries sourcing Analytics rather than recomputing):**

1. **The Monthly report's narrative section must reuse `MonthlySummary.narrative` verbatim; Reports must never independently regenerate a monthly narrative.** 4a's `MonthlySummary` (Feature 3) already produces exactly the text a Monthly PDF report would otherwise need: a grounded, once-generated, persisted, zero-fabricated-figures recap of a closed month, already reviewed under 4a's design-stage *and* pre-release Security Architect gates. Reconstructing that narrative independently inside Reports would duplicate already-approved logic (against this project's own "avoid duplication" standard) and would open a *second* AI-generation code path inside a phase this roadmap explicitly scoped as having none at all ("a monthly PDF report and a low-balance email are just aggregation + rendering/delivery, not intelligence" — this Phase 4 section's own split rationale, point 2, above). That framing is exactly why 4b's Security Architect review is scoped below to *authorization* and *email/PII content*, not a second prompt-injection/grounding review theme — a new `lib/ai/` call site inside 4b would silently invalidate that scoping and expand the review gate no one budgeted for. **Binding constraint: Reports and Notifications v2 must not call `lib/ai/` directly, at all.** Where the Monthly report includes narrative text, it is a straight, read-only lookup of the already-persisted `MonthlySummary` row for that month — the narrative section is simply omitted (not regenerated) when that row's `narrative` is null, mirroring Feature 3's own degraded-state precedent, and the report's numeric/tabular sections render fully regardless. The report's actual numeric/tabular content, for all six report types, is sourced independently from the same underlying Dashboard/Analytics/Debt/Investment/Budget services every other feature already reads from — **not** from `MonthlySummary.citedFigures`, which is a narrow JSON blob scoped only to what that specific narrative happens to mention (top 1–2 categories, one largest purchase) and is nowhere near complete enough to serve as a report's data source; treating it as a shortcut would be exactly the kind of narrow-schema trap Risk #1 warned about generally. If the Solution Architect's spec/architecture pass surfaces a genuine need for new AI-generated content inside 4b that this constraint doesn't accommodate, that is a scope question to bring back to the CTO, not a decision to make silently inside the architecture pass.
2. **Notifications v2's "large purchase" and "low balance" triggers are deterministic numeric-threshold comparisons only, with zero AI involvement** — they must not be implemented by calling into Spending Insights' (4a, Feature 4) AI-selected "notable merchant spend" logic, even though the two sound related. Spending Insights is a periodic, refresh-based, AI-narrated surface built under its own cost/latency-bounded architecture (Risk #2's constraints); a notification trigger needs to fire deterministically and near-instantly off a plain transaction-amount or account-balance comparison. Coupling the two would either force Spending Insights into a real-time role it wasn't designed for, or force this codebase's most latency-sensitive, highest-frequency-check surface to depend on an LLM call for what should be a simple threshold query. Keep them fully independent; some overlap in what a user sees between the two (a large purchase might show up in both a notification and, separately, an insight) is expected and fine, not a defect.

**Together, these two constraints mean 4b introduces zero new AI/LLM call sites and requires no AI Engineer involvement** — this makes explicit, rather than merely implied, what this Phase 4 section's original 4a/4b split rationale already assumed. The Solution Architect + Database Architect pass (milestone 3 below) needs no AI Engineer participation; 4b's eventual Security Architect review stays scoped to report/download authorization and email content/PII/deliverability exactly as already written below — it does not grow a second AI-review theme.

**The two flagged cross-cutting technical decisions below (PDF library, email provider) are confirmed as still the right decisions to defer to the Solution Architect. One added piece of context from 4a's build is worth passing along — not a new constraint, context for weighing the existing one:** 4a's cron routes (`monthly-summary`, `financial-health-score-snapshot`, and the auto-categorization batch job) are this codebase's first proven, live examples of scheduled background work succeeding on this same Vercel deployment target. If the Solution Architect's evaluation of PDF rendering approaches concludes that even a lightweight, non-Chromium renderer is still too slow/heavy to run synchronously inside a user-facing request ("click download, get a file immediately"), 4a's cron pattern is now an available, already-proven fallback shape (e.g. pre-generate monthly/yearly reports on a schedule and notify when ready, the same shape as the `MonthlySummary` job) — weigh it, don't default to it; on-demand synchronous generation remains the simpler, preferred default if a suitable lightweight renderer makes it feasible within Vercel's execution-time limit. The email provider constraint (Risk #17) is unaffected by 4a's actual shape and stands exactly as originally written — 4a shipped no email-adjacent infrastructure of any kind, so there is nothing new to weigh there.

In scope:
- **Reports**: PDF generation — monthly, yearly, tax summary, income, expense, cash flow. The monthly report's optional narrative section reuses `MonthlySummary.narrative` per binding constraint 1 above; all six report types' numeric/tabular content is independently sourced from existing Dashboard/Analytics/Debt/Investment/Budget services, never from `MonthlySummary.citedFigures`.
- **Notifications v2**: new trigger types (goal achieved, large purchase, low balance, monthly summary — see the "weekly" scope ambiguity flagged above) extending the existing Phase 2 `Notification` model and in-app infrastructure; new delivery channel — email. "Large purchase" and "low balance" are deterministic threshold triggers per binding constraint 2 above; "goal achieved" reads Financial Goals' (3b) existing completion state; "monthly summary" is a thin notify-and-link wrapper around 4a's existing `MonthlySummary` generation, not a new narrative.

**Cross-cutting technical decisions flagged, not made here** (both are the Solution Architect's to resolve during 4b's architecture pass — the CTO is setting the constraints that bound each choice):
  - **PDF generation library.** This codebase deploys to a serverless target (Vercel, per Phase 0's deployment decision). Headless-browser-based PDF rendering (e.g. a full Chromium instance) carries real constraints in that environment — cold-start latency, function size limits, execution-time limits — that a lighter, non-browser JS-native renderer would not. The Solution Architect must weigh this deployment constraint explicitly against rendering fidelity needs (charts/tables in the reports) when choosing an approach; this is a scalability concern (Risk #5) as much as a library pick. See the kickoff-pass note above: 4a's now-proven cron infrastructure is an available fallback shape (pre-generate on a schedule) if on-demand synchronous rendering can't be made to fit Vercel's limits with an acceptably light renderer.
  - **Email delivery provider.** This codebase has **no email-sending infrastructure of any kind today** — not even for auth (Better Auth's email verification, if used, and this feature are both greenfield). This is a foundational choice (transactional email provider, API keys/secrets management, deliverability, unsubscribe/preference handling for compliance) that the Solution Architect makes jointly with the Backend Engineer, within a constraint the CTO is setting now: financial-content emails (balances, summaries) are itself a new data-egress surface and must be scoped by the same "no cross-user leakage" discipline as every other feature (Risk #4), plus a new **Security Architect review focused specifically on email content** (see Risk #17 below) before release. Per binding constraint 1 above, any narrative text an email includes (e.g. a "monthly summary" notification email) is likewise a verbatim reuse of already-persisted `MonthlySummary.narrative`, never independently composed by a new call into `lib/ai/`.

### CTO resolution pass (2026-07-27): specs reviewed against kickoff constraints, no conflicts found, milestone 3 cleared to begin

Per this phase's own milestone 2 (and every prior phase's identical step — 3a/3b's own "CTO resolution pass," 4a's Financial Health Score scope resolution) — read `docs/product/reports.md` and `docs/product/notifications-v2.md` in full against this section's own kickoff-pass constraints (above) before authorizing the Solution Architect + Database Architect pass. This was a spot-check, not a full re-derivation, per the standing "trust but verify" discipline this pass has always operated under — the Product Owner did the drafting work; this pass exists to catch drift or reopened scope, not repeat their work.

**Binding-constraint compliance — confirmed, both specs.** Neither spec calls, references, or implies a new `lib/ai/` call site anywhere. This was the one thing worth being strict about, since it's the exact failure mode this kickoff pass was designed to prevent:
- Reports states the "never call `lib/ai/`" constraint verbatim at the top of the document and restates it three separate times (Cross-Cutting Requirement #3, the Monthly Report's narrative section, and the Definition of Done's "verified by construction, not convention" test requirement — no code path anywhere in report generation capable of calling `lib/ai/`). The only narrative anywhere across all six report types is a read-only `MonthlySummary.narrative` lookup, omitted when null.
- Notifications v2 states both binding constraints verbatim at the top and threads them through every trigger: Large Purchase AC3 and Low Balance AC5 both explicitly disclaim any Spending Insights/Analytics/AI call ("purely a numeric comparison, no AI involvement"); the Monthly Summary trigger's AC4 requires exact, verbatim `MonthlySummary.narrative` reuse only, never a paraphrase. No trigger introduces a second AI-narrated surface, and the Definition of Done requires the same "no code path" test discipline as Reports.

**Cross-doc consistency between the two new specs — confirmed, no conflict.**
- `MonthlySummary.narrative` is treated identically in both specs: verbatim reuse, omitted (not faked, not replaced with a placeholder) when null, never regenerated. Reports' Monthly Report section and Notifications v2's Monthly Summary trigger AC4 describe the exact same reuse contract in matching language, and both correctly cite `ai-features.md` Feature 3's own degraded-state precedent rather than inventing a new one.
- "Large Purchase" (Notifications v2's deterministic threshold-crossing trigger) and "Largest Purchases" (Reports' Analytics-sourced figure, appearing in the Yearly and Expense Reports) are, on inspection, kept cleanly separate — different terms (a singular threshold-crossing event vs. a plural period-superlative figure), no cross-reference between the two documents, and Notifications v2's Large Purchase AC3 explicitly disclaims any Analytics dependency for its own trigger. A reader moving between both documents would not confuse the two; neither spec conflates them. Worth a light naming-adjacency flag for whoever eventually writes user-facing UI copy (the same class of flag `ai-features.md` raised for the two "Health Score" surfaces, Feature 5 Reasoning point 6) so a notification and a report table sitting near each other in the UI don't read as duplicates — but this is a UI-copy note for the Frontend Lead's eventual pass, not a spec defect, and requires no Product Owner rework.

**Consistency against already-shipped product docs — spot-checked, confirmed.**
- `ai-features.md` Feature 3: Reports' "`MonthlySummary` exists for the month but its narrative is null" edge case and Notifications v2's "no row exists yet, or the row's narrative is null" edge case are both consistent with the actual shipped `MonthlySummary` schema (`prisma/schema.prisma`) — a row is created per user per closed month by the cron, `narrative` is nullable, and null is the documented "generation has not yet succeeded" signal. Neither new spec invents a data state the shipped schema doesn't actually support, and the partial-first-month handling in both specs matches Feature 3's own edge case verbatim.
- `financial-goals.md`: all three goal types' completion criteria, as restated in Notifications v2's Goal Achieved trigger, match the shipped spec's own wording exactly (Debt Payoff: balance reaches $0; Net Worth/Savings Target: measured value meets or exceeds target; Savings Rate Target: rolling 3-month average meets or exceeds target). The Goal Achieved trigger's interaction with `financial-goals.md`'s own "linked Debt archived while goal is active" freeze behavior is not contradicted — a frozen, non-completing goal simply never reaches the trigger's firing condition, which is the correct, consistent outcome without either spec needing to say so explicitly.
- `calendar-and-notifications.md`: the "bill already Late at launch still notifies" precedent and the "one active over-budget notification per category per month" anti-spam precedent are both cited accurately, and Notifications v2 correctly identifies where its own Goal Achieved trigger *diverges* from the first precedent (a deliberate, reasoned no-retroactive-fire rule, explicitly distinguished from Low Balance's opposite — and correctly precedent-matching — choice) rather than silently being inconsistent with it.
- `transactions.md`: the split-parent-row amount-is-purely-informational exclusion (AC14, via `ai-features.md` Feature 1 AC8's own already-reviewed citation of that same rule) is applied consistently by Notifications v2's Large Purchase AC1.
- `accounts.md`: Low Balance's Checking/Savings/Cash-only eligible-account scoping matches AC1's seven account types and the balance-sign convention (AC6) exactly — Credit Card's exclusion (balance represents money owed, not available) and Investment/Retirement/Crypto's exclusion (manually-entered portfolio value, not cash-flow risk) both hold up against the shipped account-type semantics, and the "no credit-limit field on `Account`" claim is accurate against AC1's field list.
- `dashboard-overview.md` / `analytics.md`: Reports' "month to date" labeling convention (`dashboard-overview.md` AC2/AC3), the zero-income savings-rate exclusion (`dashboard-overview.md` AC6), and the shared reporting-period presets (`analytics.md` AC2) are all cited accurately.

**Weekly vs. monthly resolution — ratified as written, no further CTO input needed.** The Product Owner's decision to drop Weekly Summary from this phase's scope, keeping Monthly Summary as the only summary-notification trigger, is exactly the outcome this kickoff pass flagged as the likely, defensible call if no genuine weekly data source or product demand existed — and none does. The reasoning in `notifications-v2.md`'s Scope Resolution section (no existing weekly aggregation anywhere in the codebase; building one would require either a net-new computation engine or a second `lib/ai/` call site, neither budgeted for this phase; no validated demand yet, unlike Monthly; the door is explicitly left open for a future dedicated spec if Monthly's own adoption metrics later justify it) is sound on its own terms and fully consistent with this document's binding constraints. Standing CTO authority over scope was exercised in the review above, not withheld by default — I have nothing to add or override here, and no open disagreement to record.

**Conclusion: no conflicts found, no gaps requiring a Product Owner follow-up dispatch.** Both specs stayed inside the binding constraints set at kickoff, are internally consistent with each other, and are consistent with every already-shipped product doc they reference. **Milestone 3 (Solution Architect + Database Architect combined pass) is cleared to begin**, exactly per the build order already set out in the Milestones list above — no changes to that build order are needed as a result of this pass.

**Milestones (build order and why) — confirmed unchanged from the original scope; this kickoff pass adds the reuse/integration constraints above, not a new build order:**
1. **Product Owner spec pass** for Reports and Notifications v2 — two related but independent domains (both consume already-existing data; neither depends on 4a), dispatchable in parallel, same pattern as 3a's independent domains. Must resolve the "weekly vs. monthly summary" scope ambiguity flagged above as part of this pass, before architecture.
2. **CTO resolution pass** for any cross-doc conflicts the specs surface (standing pattern).
3. **Solution Architect + Database Architect** combined pass: resolves the PDF library and email provider questions above within their stated constraints; extends the `Notification` model/enum for the new trigger types; designs the email-sending module boundary (kept separate from `lib/ai/` and from the PDF renderer — three independent concerns, not one grab-bag utility module). No AI Engineer participation is needed for this pass, per the binding constraints above.
4. **Backend implementation:** PDF report generation first (no dependency on the email provider decision) — its optional Monthly-report narrative section is a straight `MonthlySummary` database read, not a new `lib/ai/` call, so it introduces no new dependency or review surface. Then Notifications v2's new triggers and the email delivery channel; "large purchase" and "low balance" are built as plain deterministic threshold checks, independent of Spending Insights' code paths.
5. **Frontend**: report download/export UI, notification preference settings (which triggers, which channels).
6. **Full 4b review gate**: Security Architect (report/download authorization — a user must never be able to generate or fetch another user's report; email content/PII exposure and unsubscribe compliance), Performance Engineer (PDF generation cost/latency per Risk #5), Bug Hunter, full live end-to-end browser verification, Release Manager sign-off. **4c's architecture does not start until this gate passes.**

**Status: done. Review gate passed — Security Architect, Performance Engineer, and Bug Hunter all APPROVE; Release Manager APPROVE (after one REJECT/fix cycle on the `SAVINGS_RATE_TARGET` backfill gap, Risk #24) — see `docs/release/phase-4b-notes.md` and `phase-4b-checklist.md`.**

---

## Phase 4c — Calendar v2, Customization, Admin
**Goal:** the app becomes something a user can make feel like their own, and the team gets the operational tooling to run it — closing out Phase 4.

### CTO kickoff pass (2026-07-29): scope resolved for all three sub-areas, two genuine gaps surfaced and closed

Phase 4b's Reports and Notifications v2 shipped and passed its full review gate (Security Architect, Performance Engineer, and Bug Hunter all APPROVE; Release Manager APPROVE — `docs/release/phase-4b-notes.md`/`phase-4b-checklist.md`), so per this roadmap's own phase-gate rule, 4c's architecture may now begin — starting, as every prior sub-phase has, with this scope/sequencing pass rather than jumping straight to Product Owner. Unlike 4b's kickoff (which found the existing scope basically sound and only flagged one ambiguity for the Product Owner to resolve), 4c's own scope as originally written is thinner in one specific place — "Admin" is close to a bare feature list with no scale-appropriate boundary drawn around it — so this pass makes that call decisively, per this project's standing pattern of the CTO resolving these rather than deferring them to a Product Owner who would otherwise have to guess.

**1. Calendar v2 — confirmed as a zero-new-schema view extension; one wording ambiguity traced and closed.** The roadmap bullet lists "recurring transactions, budget reset, and paydays." Read against Bills' own product spec (`docs/product/bills.md`'s Business Value section, which explicitly teases "recurring transactions" as a distinct future domain from Bills — "its recurring-schedule modeling establishes a pattern later phases can extend (recurring transactions, recurring income)"), this phrase could plausibly be misread as a new, generic recurring-transaction tracking domain — e.g. recurring transfers or subscription charges modeled independently of Bills, requiring new schema. **It is not, and it does not.** No such domain has ever been spec'd (`docs/product/*.md` has no `recurring-transactions.md`), and none is needed: "recurring transactions" here is Calendar v2's own umbrella term for the same Bills occurrence data Calendar v1 already renders (`docs/product/calendar-and-notifications.md`), carried forward unchanged. **Calendar v2 is confirmed to be composed of exactly three already-existing, already-reviewed data sources, and introduces zero new Prisma models:**
   - **(a)** Bills' recurring occurrences — Calendar v1's existing scope, unchanged.
   - **(b)** Recurring Income's occurrences ("paydays") — deferred from v1 exactly as this Phase 4 section's stale-scope-resolution note above already states, now unblocked since Recurring Income shipped in 3a.
   - **(c)** A "budget reset" marker — a visual annotation of Budgeting's existing monthly-cycle boundary (the same month-boundary concept the Dashboard's month-to-date range resolution and Budgeting's own month navigator already compute), not a new stored event.

   This is a read-only aggregation layer exactly like Calendar v1 was, just composing three sources instead of one, and it is therefore the lowest-risk, fastest-to-build of the three sub-areas — a fact that shapes the sequencing confirmation below.

**2. Customization — scoped down from its literal wording to match this app's actual scale; one previously-undocumented schema gap surfaced.** "Themes, currency display, dashboard layout/widgets, accent colors," read literally, invites two kinds of scope creep disproportionate to a personal finance dashboard with a fixed, known set of dashboard cards and a single-currency (USD-only) data model, plus one genuine gap this pass found by inspecting the schema directly:
   - **"Dashboard layout/widgets" is scoped to show/hide + reorder the existing fixed set of dashboard cards** (Net Worth, Cash Flow, Savings Rate, Remaining Budget, Budget Health Score, Financial Health Score, Monthly Summary, Spending Insights, Net Worth History, etc. — the full card set as it exists after 4a/4b), stored as an ordered per-user preference list. It is explicitly **not** a generic drag-and-drop widget-builder framework, a pluggable third-party-widget system, or a per-widget-configurable-parameters system — none of that is warranted at this app's card count or its scale, and building it would be speculative infrastructure ahead of any demonstrated need.
   - **"Currency display" is scoped to a display-formatting preference only** — which currency symbol/locale number format renders already-USD-denominated amounts (e.g. `$1,234.56` vs. a different symbol/grouping convention) — **not** a multi-currency data model. Confirmed by inspecting `prisma/schema.prisma` directly: no `Account`, `Transaction`, or any other model has a currency field anywhere in this schema — there is nothing to convert *from*. Real multi-currency support (per-account currency, exchange-rate-aware conversion at display/aggregation time) would be a materially larger, cross-cutting change touching Accounts, Transactions, Net Worth, and every Analytics/Report aggregation — out of scope for this phase, and not silently implied going forward.
   - **A genuine, previously-undocumented gap, surfaced by this pass: there is no per-user timezone-preference field anywhere in the current schema.** Risk #8 (Phase 2) states its mitigation as computing date-boundary logic "against the user's stored timezone preference, not server time" — but a schema grep at this kickoff found no timezone column on `User` or any other model. Either Phase 2 implementation simplified to "assume UTC" without the risk register being corrected, or the field was never added. Either way, Customization's new per-user preferences model (which the Solution Architect is already tasked with designing, per the existing "Cross-cutting technical decision" note below) must **newly add** this field, not merely "reuse" one as Risk #8's existing text assumed — see the Risk #8 extension note added to `risk-register.md`. This is the correct place for that gap to finally get closed, but the Solution Architect should not be the first to discover it mid-implementation.

**3. Admin — the phase's one genuinely thin scope item, resolved here, decisively.** The question posed for this pass: is this a single-user admin/settings area, or does this project have real multi-user, role-based admin needs? **Resolved: neither extreme — the correct scope sits in between.** `prisma/schema.prisma`'s `User` model and `src/lib/auth.ts`'s Better Auth configuration were both inspected directly for this call: there is no `role`, `organization`, or `household` concept anywhere in the schema, every domain query is already scoped by a single authenticated user's own ID (per Risk #4's standing discipline), and Better Auth is configured for ordinary individual sign-up (email/password + Google) with no admin-adjacent plugin currently wired in. At the same time, this is unambiguously **not** a single-user app — Better Auth's signup flow, the per-user category-seeding hook, and every "scoped by `userId`" query in this codebase all assume and support many independent, unrelated end users, exactly the shape a real consumer product has. **Admin, therefore, is a small internal-operations tier for the team running this product, layered on top of that many-independent-users model — not a multi-tenant, per-organization RBAC system, and not a single-user local settings page:**
   - **A single flat privilege tier** (e.g. a `role` field on `User` scoped to `USER | ADMIN`, or an equivalent mechanism — the Database Architect's call, see below) — no per-resource permissions, no tiered admin roles (e.g. no "read-only support staff" tier this phase). This is a deliberate, scale-appropriate call, tracked as its own risk-register row in case a finer-grained need emerges later.
   - **No self-service admin-role-assignment UI.** Granting the `ADMIN` tier to a person is an operational action (a seed script or a direct database update performed by this team), not a feature end users or even admins provision through the product's own UI — "view users" in the roadmap's Admin bullet means *read* access to the user list/audit logs, not a role-management console. The Product Owner's Admin spec should state this explicitly rather than leave "manage" ambiguous about *who* gets managed.
   - **A genuine, previously-undocumented dependency, surfaced by this pass: Admin's "manage categories" (the global starter-category template) cannot function as currently built.** `src/lib/auth.ts`'s signup hook reads `DEFAULT_CATEGORIES` from `src/features/categories/default-categories.ts` — a hardcoded TypeScript constant, not a database row. An admin screen cannot edit a compiled code constant at runtime; every "edit" would require a code change and a redeploy, defeating the point of an admin tool. **This phase's Solution Architect + Database Architect pass must add a DB-backed system-category-template model** (shape TBD, their call) and migrate the signup hook to read from it, as a genuine prerequisite for this one Admin capability — not an incidental detail. See the new risk-register row below.
   - **"Seed demo data" needs an explicit target/blast-radius scope before implementation**, not left implicit. The Product Owner's spec must state whether this seeds a dedicated demo user, is restricted to non-production environments, or both — an unscoped version of this capability is a real risk (accidentally overwriting a real user's data, or an unbounded data-generation vector reachable in production). See the new risk-register row below.
   - **The authorization *mechanism*** (a `role` column vs. a separate `AdminUser`/permissions table vs. adopting Better Auth's own official `admin` plugin, which already ships a role field plus ban/impersonate primitives) **remains the Solution Architect's and Database Architect's call**, exactly as the "Cross-cutting technical decision flagged, not made here" note below already states — this pass narrows the *scope* the mechanism must serve (a single flat tier, not RBAC), not the mechanism itself. Better Auth's own `admin` plugin is flagged here as a candidate worth weighing, since it's already this codebase's auth foundation — a genuine option, not a mandate.

**Binding constraint confirmed for all three sub-areas, mirroring 4b's explicit "zero new AI call sites" declaration:** none of Calendar v2, Customization, or Admin has any reason to call `lib/ai/` — nothing in any of the three touches AI-generated content. Exactly as 4b did, **this pass confirms zero new `lib/ai/` call sites for 4c and no AI Engineer participation needed**, for either the Solution Architect + Database Architect pass (milestone 3) or the Backend implementation milestone. If the Product Owner's spec pass somehow surfaces a genuine AI-adjacent need in any of the three (none is anticipated), that is a scope question to bring back to the CTO, per the same standing rule 4b established.

**Sequencing confirmed, no changes needed to the existing Milestones build order below** — the scope resolutions above only reinforce it. Calendar v2's now-confirmed zero-schema nature makes it genuinely the fastest and lowest-risk of the three, consistent with building it in parallel with Customization, both independent of Admin and of each other, exactly as already written. Admin's authorization layer still goes first among Admin's own sub-capabilities, since view users/audit logs/feature flags/manage-categories all depend on it — and manage-categories additionally now depends on the new system-category-template model identified above, which should be designed in the same Solution Architect + Database Architect pass as the authorization mechanism, not as an afterthought once backend implementation is already underway (milestone 3, below, is updated accordingly).

In scope:
- **Calendar v2**: recurring transactions, budget reset, and paydays (see the scope-completeness note above — paydays were deferred from Calendar v1 pending Recurring Income, which shipped in 3a; see the CTO kickoff pass above for the resolved, zero-new-schema scope of all three)
- **User customization**: themes, currency display, dashboard layout/widgets, accent colors (custom categories excluded — see the resolved stale-scope note above; see the CTO kickoff pass above for the resolved scope boundaries on dashboard widgets, currency display, and the newly-surfaced timezone-preference gap)
- **Admin**: seed demo data, manage categories (the global starter-category template — see the resolved scope note above), view users, audit logs, feature flags (see the CTO kickoff pass above for the resolved single-flat-role authorization scope, the category-template schema dependency, and the demo-data guardrail requirement)

**Cross-cutting technical decision flagged, not made here:** the shape of admin authorization. No `role`/permission concept exists on `User` today — this is a net-new, foundational authorization tier for the app, not a detail to be improvised inside an Admin feature spec. The Solution Architect and Database Architect must decide the mechanism (a `role` field on `User`, a separate `AdminUser`/permissions table, adopting Better Auth's own `admin` plugin, etc.) as its own explicit, reviewed milestone — the same discipline Risk #9 required for 3a's Account-linkage decision — before any Admin backend work starts. **Narrowed by the CTO kickoff pass above: whatever mechanism is chosen must serve a single flat privilege tier (no per-resource RBAC, no self-service role assignment) — the scope call is made; the mechanism is still this pass's to make.**

**Recommendation for the Solution Architect's consideration (not a mandate):** since Admin's feature-flag capability could plausibly have been useful to gate 4a's AI rollout or 4b's email delivery if either needed to be disabled quickly post-launch, the architecture pass should consider whether a minimal, standalone feature-flag primitive is worth building as a small cross-cutting utility rather than being scoped as "just another Admin screen" — it may be useful sooner and more broadly than the rest of Admin. **Still stands, unaffected by this kickoff pass.**

### CTO resolution pass (2026-07-29): specs reviewed against kickoff constraints and against each other — one genuine scope overreach found and descoped, three Admin open questions decided, one dependency gap confirmed and routed

Per this phase's own milestone 2 (and every prior phase's identical step — 3a/3b's resolution pass, 4a's Financial Health Score scope resolution, 4b's resolution pass immediately above) — read `docs/product/calendar-v2.md`, `docs/product/customization.md`, and `docs/product/admin.md` in full against this section's own kickoff-pass constraints (above) before authorizing the Solution Architect + Database Architect pass. Unlike 4b's pass (which found the two specs basically sound and only made minor observations), this pass found one genuine scope problem worth resolving decisively rather than letting ride, plus three explicitly-flagged Admin open questions and one explicitly-flagged dependency gap that all needed an actual decision, not a "looks fine" spot-check.

**Binding-constraint compliance — confirmed, all three specs.** None of Calendar v2, Customization, or Admin calls, references, or implies a new `lib/ai/` call site anywhere — Admin's Audit Log capability only *surfaces* records of AI usage 4a/4b already produce (Capability 3 AC1), it never calls a model itself, exactly as the kickoff pass required. No AI Engineer participation is needed for the Solution Architect + Database Architect pass or backend implementation, confirming the kickoff pass's own declaration.

**Cross-doc consistency between the three specs — confirmed, no conflicts, aside from the timezone question resolved separately below.** Calendar v2's zero-new-schema, three-source composition (Bills, Recurring Income, Budgeting's month boundary) is consistent with Customization's own framing of the same three sources, and both are cleanly independent of Admin (Admin touches user accounts, categories, flags, and audit data — never a bill, payday, or budget figure). Customization's dashboard-card enumeration matches the actual shipped card set through 4a/4b (Financial Health Score, Monthly Summary, Spending Insights, Net Worth History all present and correctly attributed to their shipping phase). Admin's Feature Flags AC2/AC3 correctly describe 4a's and 4b's own already-reviewed degradation behavior (AI "unavailable" state, email-failure in-app-only fallback) rather than inventing a new broken state. Admin's Manage Categories capability is disambiguated from Phase 1's per-user category CRUD identically in both `admin.md`'s own scope-carryover section and this Phase 4c section's stale-scope note above — no drift. Risk #26 (seed-demo-data blast radius) and Risk #28 (dashboard-widget/currency scope) are both resolved in `admin.md`/`customization.md` exactly as the kickoff pass anticipated, with no gaps — see the risk-register updates below.

**The headline issue — Customization's Timezone Preference AC2/AC4 overreached beyond what the kickoff pass actually authorized, and it is also the answer to Calendar v2's own flagged Open Question. Decided here, not left hanging.**

Calendar v2's spec explicitly declines to resolve its own Open Question: does its timezone-correctness depend on Bills/Recurring Income/Budgeting's date-boundary logic actually being updated to consume Customization's new timezone field this phase, or is that a separate, later concern? It asks this pass to decide. Customization's spec, unprompted, answers this itself in its Timezone Preference capability's (draft) AC2: it commits to updating date-boundary logic across nine already-shipped, already-reviewed features (Bills, Budgeting, Recurring Income, Dashboard month-to-date figures, Analytics' reporting-period control, Automatic Monthly Summaries, Notifications v2's monthly trigger, Financial Goals' Savings Rate rolling window, Reports' period boundaries, Net Worth History's "as of" labeling) plus four cron jobs (Monthly Summary, Net Worth Snapshot, Financial Health Score Snapshot, `evaluate-notifications`) to evaluate per-user timezone boundaries this phase.

**Decision: descope. The timezone field ships in 4c; the nine-consumer-plus-four-cron rewiring does not, and is deferred to a dedicated, later, explicitly-scoped pass.**

Reasoning:
1. **This phase's own stated goal is personalization/polish, not a backend correctness overhaul.** This section's own Goal line frames 4c as the phase where "the app becomes something a user can make feel like their own" — a fixed-card-set show/hide/reorder, an accent color, a currency display format, a new settings field. Rewiring nine already-shipped features' core date-boundary logic plus four cron jobs is not a personalization feature at all — it is a correctness migration touching nearly every date-sensitive surface in the product, bundled into the same review cycle as three genuinely new, much smaller capabilities. That is a fundamentally different *kind* of work than the rest of this phase, not just a bigger version of it.
2. **Every one of those nine consumers already shipped and already passed its own review gate**, each with its own test suite built around today's server/UTC-time assumption. Touching all nine in one pass — plus the four cron jobs, which are this codebase's only externally-scheduled, per-user-fanned-out background work — is a materially higher-regression-risk undertaking than adding a new preferences table and a new settings page. It risks re-litigating (and potentially re-breaking) Bills' Late-status logic, Budgeting's monthly reset, Recurring Income's occurrence generation, four Dashboard figures, Analytics' period presets, two AI-adjacent gates (Monthly Summary generation, Notifications v2's monthly trigger), a Financial Goals completion window, Reports' period boundaries, and Net Worth History's labeling — nine independent blast radii — in a single review cycle already carrying Calendar v2's three-source composition, Customization's other three preferences, and all six of Admin's capabilities.
3. **This is exactly the class of risk this project's own phase-splitting precedent exists to avoid.** The 3a/3b split, the 4a/4b/4c split, and 4b's own AI-isolation reasoning were all justified on "don't bundle a heterogeneous, higher-risk change in with lower-risk, already-well-understood work just because they're topically adjacent." A nine-feature-plus-four-cron date-logic rewrite bundled into "themes and accent colors" is precisely that mistake, just discovered one phase later than usual.
4. **The alternative genuinely delivers value without the regression exposure.** Shipping the field itself — selectable, persisted, browser-inferred at signup with a UTC fallback, editable like any other preference — is real, complete, tested work on its own terms; it is simply not yet *consumed* by anything. This mirrors 3a's own precedent of beginning Net Worth Snapshot capture before the chart that would display it existed: infrastructure landing ahead of the feature that consumes it is an established, low-risk pattern in this codebase, not a new one.
5. **Nothing about this call requires re-deciding when the deferred work happens.** It is deferred to a dedicated, later, explicitly-scoped pass, to be scheduled once there has been time to design a lower-regression-risk, per-consumer migration strategy (e.g. shadow-read verification against each already-shipped feature's existing test suite before cutover, rolled out feature-by-feature rather than as one big-bang change) — not folded silently into whatever ships next, and not left permanently unscheduled either.

**Action taken:** `docs/product/customization.md`'s Timezone Preference capability was edited directly (a resolution pass has the same standing authority to adjust a spec after finding a genuine scope problem that the Phase 4b resolution pass would have had, had it found a conflict). A "Scope note" now precedes the Acceptance Criteria explaining the descope; AC2 and AC4 are retained verbatim as target-state design (marked "deferred," not deleted, so the Product Owner's analysis work is preserved for whenever the future pass picks it up) rather than implemented this phase; the Definition of Done, Dependencies, and Success Metrics sections were each adjusted to stop claiming a cross-feature correctness bar this phase does not actually meet. Risk #8 was extended with this decision, and a new Risk #29 was added to track the deferred work itself as its own durable register row (distinct from Risk #8, which is about the original Phase 2 mis-documentation, not the now-explicit backlog item). See `risk-register.md`.

**Calendar v2's Open Question is resolved by the same decision:** the timezone field lands as schema/preference groundwork this phase; Bills, Recurring Income, and Budgeting's own date-boundary computations are not updated to consume it in 4c. Calendar v2 therefore continues rendering dates exactly as its three sources compute them today (server/UTC-based) — precisely the "Calendar v2 should continue doing the same" branch its own Open Question already anticipated as the likely outcome if the source features weren't updated. No edit to `calendar-v2.md` itself was needed; its Open Question already correctly described both branches and deferred the decision to this pass, which it now has.

**Admin's three Open Questions — each decided or explicitly deferred, not left hanging.**

1. **Audit log retention: decided — indefinite retention for 4c, revisit later if a real storage signal emerges.** The audited event set is small and bounded (AI-feature usage, report generation, notification/email sends, category-suggestion decisions, per Admin Capability 3 AC1/AC5 — explicitly not a blanket audit-everything log), so building expiration/archival infrastructure now would be speculative ahead of any demonstrated need, the same call already made for dashboard-widget frameworks and multi-currency support (Risk #28). Tracked as new Risk #31 so "forever, by default" is an explicit decision, not a silent default.
2. **Feature-flag governance: decided — no formal registry process; a standing per-phase-kickoff evaluation instead.** Rather than build process/tooling for a hypothetical future flag before a second flag is ever needed, every future phase's own CTO kickoff pass (the same step this document already performs for every phase) will explicitly evaluate whether that phase introduces a new high-risk automated/data-egress surface and, if so, decide then whether it earns a flag — registered against the standalone feature-flag primitive the Solution Architect's 4c pass is already asked to weigh (kickoff pass, above). This is the same case-by-case discipline already used to scope each phase's Security Architect review; it does not need its own bespoke process. Tracked as new Risk #32.
3. **"Last active" definition: decided — most recent `Session.updatedAt` across a user's sessions, falling back to `createdAt` if a session has never been renewed.** Better Auth's `Session` model (`prisma/schema.prisma`) already carries both `createdAt` (login/session-creation) and `updatedAt` (touched on session renewal); using only `createdAt` would show a user who logged in once and stayed on one long-lived, quietly-renewed session as "inactive" for months despite daily use, which is the wrong signal for the operational question View Users exists to answer ("does this account still look alive"). `MAX(Session.updatedAt)` is the more honest proxy for actual recent activity. This is a product-level definition, not the full mechanism — the exact renewal cadence Better Auth applies to `updatedAt` (i.e., how often a session is actually touched under normal use) is a detail for the Solution Architect/Database Architect pass to confirm against Better Auth's own configuration, not re-decided here.

**Admin's flagged dependency gap — confirmed real, and confirmed correctly routed.** `admin.md`'s own Dependencies section already flags this rather than silently assuming it solved: Reports (4b) has no persisted row recording when a report was generated. Verified directly against the actual code for this pass, not taken on the spec's word alone: `src/features/reports/server/service.ts`'s `generateReport()` calls straight through validate → resolve period → assemble data → render PDF → return a buffer, with no Prisma write anywhere in that path; `src/app/api/reports/route.ts` likewise only returns the PDF bytes or an `ApiResult` error, no database call. `prisma/schema.prisma` has no report-generation-event model of any kind. The gap is real. It is correctly routed to the Solution Architect + Database Architect's upcoming 4c combined pass as a schema addition (a lightweight generation-event log), not silently left for a Backend Engineer to discover mid-implementation of Admin's Audit Log capability — added explicitly to milestone 3's schema-question list below, and tracked as new Risk #30.

**No other cross-doc inconsistency found.** Calendar v2's AC13 (findability, reachable from primary nav for ordinary users) and Admin's AC3 (zero nav trace for non-admins) are not in tension — they govern two different audiences (all users vs. non-admins specifically) and two different areas of the app. Customization's settings-surface requirement and Admin's separate, invisible-to-non-admins area are likewise two distinct surfaces with no overlap. Admin's Feature Flags AI/email kill switches and Customization's per-user preferences (theme, layout, currency, timezone) operate on entirely independent axes (an admin-level operational switch vs. a user-level display/personalization preference) with no described interaction that could conflict.

**Conclusion: one genuine scope problem found and resolved (the timezone AC2/AC4 overreach, descoped, `customization.md` edited), three Admin open questions decided, one dependency gap confirmed and routed. Milestone 3 (Solution Architect + Database Architect combined pass) is cleared to begin, with its schema-question list expanded from four to five (below) to include the report-generation-event log.**

**Milestones (build order and why):**
1. **Product Owner spec pass** for Calendar v2, Customization, and Admin — three domains, dispatchable in parallel (Calendar v2 and Customization are genuinely independent of each other and of Admin; Admin's audit-log spec should explicitly reference what 4a/4b now produce that's worth auditing, per the dependency reasoning above). Grounded in the CTO kickoff pass's resolved scope above.
2. **CTO resolution pass** for any cross-doc conflicts (standing pattern). **Done — see the resolution pass immediately above.**
3. **Solution Architect + Database Architect** combined pass: resolves the admin authorization-*mechanism* question above (within the kickoff pass's flat-single-tier scope); designs Calendar v2's extension of the existing calendar view (composing Bills, Recurring Income, and Budgeting's month-boundary — zero new models, per the kickoff pass above); designs the Customization preference model (a new per-user settings/preferences table, including the newly-surfaced timezone-preference field — scoped, per the resolution pass above, to the field itself, not to rewiring its nine target-state consumers); designs the new DB-backed system-category-template model Admin's manage-categories capability depends on (per the kickoff pass above); and **decides whether/how to add a lightweight report-generation-event log** so Admin's Audit Log capability has a row to surface for report-generation events, which Reports (4b) never persisted (confirmed a genuine gap by the resolution pass above, not assumed) — **five related but distinct schema questions in one combined pass, not four.**
4. **Backend implementation, sequenced:** Admin's authorization layer first (it is a prerequisite for every other Admin capability — view users, audit logs, feature flags, manage categories all need it; manage-categories additionally depends on the new system-category-template model), then Calendar v2 and Customization in parallel (both independent of Admin and of each other). **Customization's timezone-preference implementation this phase is limited to the field itself and its settings UI, per the resolution pass's descope above — not the nine-feature/four-cron consuming-logic rewrite, which is out of scope for this milestone.**
5. **Frontend**: Calendar v2 view updates, Customization settings UI (theme/currency/layout/accent/timezone), Admin dashboard screens.
6. **Full 4c review gate**: Security Architect (admin authorization is the headline concern here — privilege escalation, audit-log tamper-resistance, feature-flag access control), Performance Engineer, Bug Hunter, full live end-to-end browser verification, Release Manager sign-off.

**Phase 4 is complete when 4c's review gate passes.**

---

## Phase 5 — Motion & Craft Pass
**Goal:** "software someone would pay for."

- Number counters, chart transitions, smooth page transitions, expandable cards — applied as a dedicated pass across all existing screens, not bolted on per-feature
- Full accessibility pass (E2E Engineer + manual review)
- Full responsive audit: desktop/tablet/mobile, collapsible sidebar, bottom navigation on mobile

This phase is deliberately last: polishing screens that are still changing structurally in Phases 1–4 would be wasted or re-done work.

---

## Phase 5 CTO kickoff pass (2026-07-30)

Phase 4 is complete in full. 4c's review gate passed on its second pass (Release Manager APPROVE, no open follow-up items — `docs/release/phase-4c-notes.md`, `docs/release/phase-4c-checklist.md`), closing out every domain in Phases 1 through 4c. Per this roadmap's own phase-gate rule, Phase 5's architecture work may now begin — starting, as every prior phase has, with this scope/sequencing pass rather than dispatching the Product Owner straight off the three-bullet stub above, which (like every phase's original stub before its own kickoff pass) states a goal and a rough feature list but not the level of detail a spec pass can consume on its own.

Unlike every prior phase, Phase 5 has no new data domain and no new external dependency to architect — its "feature area" is the entire application surface built across Phases 1 through 4c. That breadth, not novelty, is what this pass has to size correctly.

### 1. Sequencing decision: split into Phase 5a (Accessibility & Responsive Foundation) and Phase 5b (Motion & Craft)

The existing stub bundles three workstreams — motion/transitions, accessibility, responsive/mobile — into a single "Phase 5." Read literally, that would be this project's largest single review-gate surface yet: every route shipped since Phase 0, touched three different ways in one pass. Splitting is the right call, for three reasons, in the same numbered-reason format every prior phase split has used (3a/3b, 4a/4b/4c):

1. **Real dependency chain, not just size — the same class of reasoning 3a/3b was built on.** Motion work (number counters, chart transitions, page transitions, expandable cards) is decoration layered on top of already-existing DOM structure and interaction patterns; it assumes that structure is stable. Accessibility and responsive work are not decoration — they are audits that routinely surface required structural changes: a missing landmark, an expandable card built as a bare `<div>` toggle that needs to become a real disclosure widget with `aria-expanded`/`aria-controls`, a data table that needs a genuinely different mobile layout (card-list fallback) rather than a squeezed-down version of its desktop grid, a new bottom-navigation component for mobile that doesn't exist in any form today. Building expandable-card animation, chart-transition timing, or page-transition wrapping *before* that structural work lands means redoing it once the underlying markup changes shape — animating a `div` that later becomes a semantically different disclosure element, or building a page-transition wrapper around a layout a responsive pass then restructures around a new bottom nav. This is exactly the "architecting X before Y's data model is settled risks designing against a moving target" risk 3a/3b's split was built to avoid, with "data model" replaced by "DOM structure."
2. **Distinct review-gate shape and headline reviewer, the same class of reasoning 4a/4b/4c's split was built on.** 5a's release gate is headlined by accessibility/responsive verification — the E2E Test Engineer's automated + manual coverage and the Bug Hunter's cross-breakpoint testing are the load-bearing review for this sub-phase, the same way Security Architect was the headline reviewer for 4a (prompt injection) and for Admin specifically within 4c (privilege escalation). 5b's release gate is headlined by the Performance Engineer — animation cost, GPU compositing, bundle-size delta from a JS animation library's wider rollout — a materially different review focus. Bundling both into one gate would either dilute the scrutiny 5a's structural changes need, or hold up 5b's comparatively lower-risk decoration work behind a slower pipeline it doesn't need.
3. **Scale argument, calibrated against this project's own "don't over-split" caution.** Phase 5 spans every screen shipped across five phases — a materially larger single-pass surface than any prior phase's review gate. That alone justifies one split. It does not justify a three-way split (motion / accessibility / responsive each on their own): accessibility and responsive share the same shape (audit existing screens, fix what's broken, no new feature, no new data model) and the same position ahead of motion in the dependency chain identified in reason 1 above — splitting them from each other would add a second full Product-Owner-spec → CTO-resolution → architecture-review cycle for two workstreams that are naturally one combined audit-and-fix pass, the same "three sub-phases is already a meaningful increase in process overhead... a fourth would push past what this team's proven cadence supports" caution 4c's own kickoff pass raised against splitting Admin out further. **Two sub-phases, not three.**

**Within 5a, responsive work is sequenced before the accessibility audit, not run in parallel with it or after it — a build-order decision, not a further split.** The reverse order (audit first, restructure for mobile second) would mean auditing the pre-mobile-nav, pre-card-list-conversion version of the app and then needing a second, narrower audit pass just for whatever the responsive work newly introduces (most concretely, the new bottom-navigation component this phase's stub already calls for — a genuinely new interactive, keyboard/screen-reader-relevant structure that doesn't exist yet in any form). Building the final structure first and auditing it once is cheaper and more honest than auditing twice.

### 2. Findings from direct inspection — grounding this pass the same way every prior kickoff pass grounded its scope calls in the actual current schema/code, not assumption

Checked directly against current source before writing this section, following the same discipline 4c's kickoff pass used when it grepped `prisma/schema.prisma` for a timezone column rather than trusting Risk #8's prose:

- **`prefers-reduced-motion` has zero handling anywhere in this codebase today** — confirmed by a direct grep of `src/` and `src/app/globals.css`: no matches at all, not in a component, not in a CSS media query, not in any hook. This is a genuine, previously-undetected gap, the same class of finding as 4c's timezone-column gap — flagged now, before any motion work multiplies it across every screen, rather than discovered mid-implementation. See new Risk #40.
- **Framer Motion is already an installed, live dependency (`framer-motion@^12.42.2`, `package.json`), and is already this codebase's named animation tool.** `ui-component-engineer.md`'s own charter already instructs that role to "Use Tailwind + shadcn/ui conventions and Framer Motion for animation primitives." No new animation-library decision is needed for Phase 5b — this removes one cross-cutting technical decision every prior phase's kickoff has had to flag (LLM provider in 4a, PDF/email libraries in 4b, admin-auth mechanism in 4c); Phase 5 has none of that shape. **However**, Framer Motion is currently used in exactly one component (`src/components/shared/progress-ring.tsx`) — 5b's "dedicated pass across all existing screens" is a real increase in that library's footprint across the client bundle, not a marginal addition, and the Performance Engineer's 5b gate review must measure that increase directly rather than assume it's already paid for. See new Risk #44.
- **This app already has a working, if partial, responsive foundation — it is not desktop-only today.** `components/shared/sidebar.tsx` already collapses to hidden below `md`, renders icon-only between `md` and `lg`, and expands fully at `lg`+; `components/shared/top-nav.tsx` already has a working hamburger-menu `Sheet` trigger below `md` that renders `<Sidebar mobile />`. 117 occurrences of `md:`/`lg:`/`sm:` Tailwind responsive classes already exist across 76 files. This matters for scoping 5a correctly: the responsive workstream is a **systematic audit and completion pass** over an existing, ad hoc foundation — verifying every route, not bootstrapping responsive design from zero. The one genuinely new structural piece the stub calls for is **bottom navigation on mobile**, which has no existing equivalent and risks becoming a second, competing mobile-nav mechanism alongside the already-shipped hamburger `Sheet` if its relationship to that existing mechanism isn't explicitly resolved before implementation. See new Risk #42.
- **No accessibility audit has happened at any point in this project's history, and the tooling this roadmap's own Phase 5 stub assumes exists does not exist.** Every prior phase's Bug Hunter reports (`docs/testing/bug-reports/*.md`, 21 files) cover race conditions, TOCTOU bugs, timezone/UTC issues, and data-integrity edge cases — none address accessibility. More significantly: the stub names "E2E Engineer" as 5a's accessibility reviewer, and `.claude/agents/e2e-test-engineer.md`'s own charter already lists "Accessibility" as one of its required Playwright test targets — but that role has never once been dispatched over the whole project. Confirmed directly: no `tests/e2e/` directory exists, no `docs/testing/e2e/` reports exist, and `playwright`/`@playwright/test` is not a dependency anywhere in `package.json`. What manual/incidental accessibility exists today is real but ad hoc — `sidebar.tsx` and `top-nav.tsx`, for instance, already carry `aria-label`, `aria-current`, `aria-hidden` on decorative icons, and a `focus-visible:ring-2` pattern — but this is whatever individual Frontend Lead/UI Component Engineer passes happened to include, not the output of a dedicated audit. **This is genuinely starting from zero as an audited, tooled practice, even though it is not starting from zero as an ad hoc one.** See new Risk #41.
- **Customization's (4c) accent-color presets and light/dark theme multiply the color-contrast surface any accessibility audit must now cover** — a combination that did not exist before 4c shipped (this app had exactly one default light/dark theme with no accent choice through Phase 4b). See new Risk #43.
- **No visual-regression or cross-breakpoint screenshot testing exists today.** Not a decision to make here — it's the Solution Architect's call whether 5a's/5b's own verification approach needs it — but flagged because a systematic responsive audit and a systematic motion pass are exactly the kind of sweeping, every-screen change that a manual, ad hoc "looks right at the one breakpoint I checked" review is most likely to under-verify. See new Risk #45.

### 3. Acceptance criteria — concrete, per workstream, binding constraints for the Product Owner spec and later review gates to check against

The existing stub's language ("smooth page transitions," "software someone would pay for") is not independently verifiable. The following are CTO-set binding constraints — the Product Owner's spec fleshes these into full acceptance criteria per screen/component, the same way 4a's spec fleshed out the CTO's structured-output/data-minimization constraints into Feature-level ACs.

**Accessibility (5a):**
- **Target level: WCAG 2.1 AA, minimum, app-wide.** Not left to the Product Owner to pick — this is the industry-standard baseline for commercial software and is what "software someone would pay for" actually cashes out to on this axis.
- Automated coverage: an axe-core-based check (e.g. `@axe-core/playwright`) integrated into a newly-bootstrapped `tests/e2e/` Playwright suite, run against every route — zero critical/serious violations at gate time; any accepted moderate/minor finding is a named, owned, tracked backlog item, not silently dropped.
- Full keyboard-only navigation verified on every interactive surface (data tables, dialogs, sheets, dropdown menus, forms): no keyboard trap, logical tab order, a visible focus indicator on every focusable element — `sidebar.tsx`'s existing `focus-visible:ring-2 ring-offset-2` pattern is the baseline every other interactive element must match, not a new bar to invent.
- Screen-reader smoke test (NVDA or VoiceOver) on every flow `e2e-test-engineer.md`'s own charter already names: authentication, dashboard, budget creation, transaction entry, account management, reports, goals, import/export.
- Color-contrast audit covers every 4c accent-color preset crossed with both light and dark themes — not just the default theme — per the finding above.
- Every form input has a programmatically associated label; every icon-only interactive control has an accessible name (`aria-label` or equivalent) — `sidebar.tsx`'s collapsed icon-only nav items are the existing correct pattern (`aria-label={item.label}`), not a new bar.

**Responsive (5a):**
- Named breakpoints: mobile `<640px`, tablet `640–1024px`, desktop `≥1024px` — matching Tailwind's own `sm`/`md`/`lg` scale already in use throughout the codebase, not a new scale invented for this phase.
- Every route in the app (a full inventory, produced as part of the Product Owner spec, not a spot-check) verified with no horizontal scroll, no clipped/overlapping content, and no functionally unreachable control at each named breakpoint.
- Every data-heavy surface (Transactions table, Reports, Analytics charts, Admin user list/audit log) gets an explicit, documented mobile-treatment decision (card-list fallback vs. horizontal-scroll-with-affordance vs. column-priority collapse) — decided once as a reusable pattern, not reinvented per feature.
- Bottom navigation on mobile has an explicit, stated relationship to the already-shipped hamburger `Sheet` mobile sidebar (replace it, or serve a distinct "most-used routes" role alongside it) — not left ambiguous, per the finding/risk above.
- Touch targets meet a 44×44px minimum (standard WCAG/iOS/Android guidance) — ties the responsive and accessibility workstreams together on this one concrete, shared bar.
- The sidebar's existing collapsed/icon-only/full states are re-verified, not rebuilt, at the tablet breakpoint specifically — the one band the existing code already treats specially.

**Motion & Craft (5b):**
- **`prefers-reduced-motion` (OS-level, at minimum) is a binding requirement of every animation this phase adds, not optional polish** — when set, motion is replaced with an instant state change, never an information-bearing animation with no static equivalent. Implemented once, centrally (e.g. `MotionConfig reducedMotion="user"` at the app shell, or an equivalent single shared utility) — not a per-component ad hoc check, per Risk #40 below.
- Number counters animate from previous to new value on data change/mount only (never re-triggered by an unrelated parent re-render), duration bounded (roughly 300–600ms), and rendered through the exact same `useFormatCurrency`/`formatCurrency` pipeline 4c's currency-display fix just finished wiring app-wide — never a second, parallel formatting path for the animated intermediate values.
- Chart transitions (Recharts) have bounded, consistent entrance/update animation duration and introduce no layout shift (CLS).
- Page transitions do not block interactivity (no Time-to-Interactive regression) and do not interfere with Next.js's existing streaming/`loading.tsx` skeletons.
- Expandable cards use one reusable primitive (owned by the UI Component Engineer, in `components/ui/` or `components/shared/`, per that role's own "reusable, never per-feature" mandate), with correct `aria-expanded`/`aria-controls` semantics — conforming to whatever disclosure-widget pattern 5a's accessibility pass already established, not inventing a second one.
- Framer Motion remains the sole animation library; CSS transitions/Tailwind utilities (already used in 34 files today) remain the default for simple hover/focus state changes — Framer Motion is reserved for orchestrated motion (page transitions, counters, expandable cards) matching each tool's actual strength, not used to replace a one-line `transition-colors` class.
- Motion uses GPU-compositable properties (`transform`/`opacity`) rather than layout-triggering properties (`width`/`height`/`top`/`left`) wherever feasible — a concrete, checkable bar for the Performance Engineer's review.
- Applied as a systematic pass across a full route inventory (produced by the Product Owner spec, same inventory 5a already had to produce) — not a per-feature bolt-on, per the stub's own "applied as a dedicated pass... not bolted on per-feature" framing.

### 4. In-scope vs. explicitly out-of-scope

**In scope:**
- Every route/screen shipped across Phases 1–4c (the full inventory named above).
- 5a: WCAG 2.1 AA accessibility pass + bootstrapped Playwright/axe-core E2E infrastructure; full responsive audit at the three named breakpoints, including a new bottom-navigation component for mobile with its relationship to the existing hamburger sidebar explicitly resolved.
- 5b: number counters, chart transitions, page transitions, expandable cards, via Framer Motion (already installed) plus existing CSS-transition/Tailwind utilities; `prefers-reduced-motion` support as a binding requirement, not optional.

**Explicitly out of scope, same discipline as every prior phase's descope calls (4c's custom-categories staleness check, Timezone consuming-logic descope):**
- **Any new data model or feature.** Phase 5 is presentation-layer work. If 5a's audit surfaces a genuine need for a persisted per-user preference (e.g., an in-app reduced-motion override beyond the OS-level media query, or a density/compact-mode preference), that is a schema question for the Solution Architect/Database Architect pass to size and for the CTO to confirm — not something decided unilaterally in this kickoff pass. Flagged as an open question below, not assumed either way.
- **A general visual/design-system overhaul.** If the color-contrast audit finds a failing token, the fix is a targeted token adjustment, not a rebrand. Phase 5 is motion/accessibility/responsive, not a redesign.
- **PWA installability / offline mode.** "Bottom navigation on mobile" means a responsive web layout change, not a service-worker-backed installable app — a materially different, much larger scope not implied by this stub.
- **Internationalization / multi-language support.** Out of scope, consistent with this app's existing English/USD-locale-only posture (the same display-formatting-only, not full-i18n, precedent Risk #28 already established for currency).
- **Backend/database performance work.** If an audit surfaces a genuine backend performance issue, that's routed to a targeted, separate Performance Engineer/Backend Engineer follow-up — not folded into this phase, the same way 4c routed newly-discovered backend gaps (the system-category-template model, the report-generation-event log) into its own dedicated architecture-pass line items rather than letting them silently expand scope.
- **A second animation library.** Framer Motion is the only tool Phase 5b uses. Introducing anything else (GSAP, React Spring, Lottie, etc.) is a new cross-cutting technical decision requiring its own return trip to the CTO, the same standing rule 4a's LLM-provider constraint and 4b's PDF/email-provider constraints already established for this project's other foundational-tool choices.

### 5. Open questions flagged for the Product Owner / Solution Architect — not decided here

- **Reduced-motion scope**: does 5b ship OS-level `prefers-reduced-motion` support only, or also a persisted in-app override (a new field on 4c's `UserPreference` model)? The former is unambiguously in scope (see Section 3's binding constraint); the latter is a schema question for the Solution Architect + Database Architect pass to size, and the Product Owner's spec should state which it's asking for rather than leave it implicit.
- **Bottom-nav relationship to the existing hamburger sidebar**: replace it on mobile, or serve a distinct "most-used routes" role alongside it? The Product Owner's 5a spec must state this explicitly, per Section 3/Risk #42.
- **Visual-regression tooling**: worth adopting for this phase's own verification, or is manual multi-breakpoint/multi-theme review by the Bug Hunter and E2E Engineer sufficient? This is the Solution Architect's call to make during 5a's architecture pass, the same way 4b's Solution Architect weighed cron-vs-synchronous PDF rendering as a real, non-obvious tradeoff rather than a foregone conclusion.

### 6. Milestones (build order and why)

**Phase 5a — Accessibility & Responsive Foundation:**
1. **Product Owner spec pass**, producing the full route/screen inventory both workstreams need, and resolving the two open questions above (reduced-motion persistence scope, bottom-nav relationship) that this pass has explicitly declined to decide unilaterally.
2. **CTO resolution pass**, standing pattern.
3. **Solution Architect (+ Database Architect only if the reduced-motion-override question above resolves to "yes, persisted")** pass: decides the Playwright/axe-core bootstrap approach (new `tests/e2e/` infrastructure per `e2e-test-engineer.md`'s own charter, never yet stood up); designs the bottom-navigation component's boundary/reuse relationship to `components/shared/sidebar.tsx`; decides on visual-regression tooling per the open question above.
4. **Implementation, responsive before accessibility, per Section 1's build-order reasoning**: Frontend Lead + UI Component Engineer complete the full responsive pass first (including the new bottom-nav component and any data-table mobile-treatment conversions), then the accessibility pass (structural fixes — landmarks, ARIA, focus order, contrast token adjustments) runs against that final structure exactly once.
5. **E2E Test Engineer** bootstraps the Playwright suite and writes the accessibility test coverage named in Section 3, against the now-final structure.
6. **Full 5a review gate**: E2E Test Engineer (accessibility automated + manual coverage — the headline reviewer for this sub-phase), Bug Hunter (cross-breakpoint stress testing), Security Architect (standing per-phase review, lighter-touch here — no new data-egress/auth surface, though the new Playwright suite's own test-credential handling is worth a quick look), Performance Engineer, Release Manager sign-off. **5b's architecture does not start until this gate passes.**

**Phase 5b — Motion & Craft:**
1. **Product Owner spec pass**, grounded in 5a's now-final, now-audited structure — defines exact motion behavior per component/screen against the route inventory 5a already produced.
2. **CTO resolution pass**, standing pattern.
3. **Solution Architect** pass: designs the shared reduced-motion utility (Section 3's binding constraint), the reusable expandable-card primitive's ownership boundary (UI Component Engineer), and any shared page-transition wrapper's composition point in the app shell.
4. **Implementation**: UI Component Engineer builds the reusable animation primitives (expandable card, number counter, page-transition wrapper) first, as the foundation the Frontend Lead then applies screen-by-screen across the full route inventory — mirroring 4a's own "foundation-first" build-order reasoning (auto-categorization before the other four AI features).
5. **Full 5b review gate**: Performance Engineer (the headline reviewer — bundle-size delta from Framer Motion's wider rollout, GPU-compositable-property usage, 60fps target, no TTI regression), Bug Hunter, E2E Test Engineer (confirm the reduced-motion path via automated test, not just manual spot-check), Security Architect (standing per-phase review), Release Manager sign-off.

**Phase 5 is complete when 5b's review gate passes.**

---

## Phase 5a CTO resolution pass (2026-07-30): spec reviewed against kickoff constraints — binding constraints honored, no scope drift, two mobile-treatment gaps found and closed directly, cleared for Solution Architect pass

Per this phase's own milestone 2 (and every prior phase's identical step — 3a/3b's resolution pass, 4a's Financial Health Score scope resolution, 4b's and 4c's resolution passes above) — read `docs/product/phase-5a-accessibility-responsive.md` in full against this section's own kickoff-pass constraints (Section 3) before authorizing the Solution Architect pass. This was a line-by-line check against the kickoff pass's binding constraints and a direct re-verification of every factual claim the spec makes against the actual codebase — the same "trust but verify" discipline every prior resolution pass has used, applied here with extra weight on the two claims most likely to silently drift (the accent-preset count, the route inventory), since both are load-bearing for downstream work (5a's own audits, and 5b's later spec, which reuses this same inventory verbatim).

**1. Binding-constraint compliance (Section 3) — confirmed, all six constraints honored, with every cited number re-verified against source rather than trusted from the spec's own prose.**
- **WCAG 2.1 AA floor**: stated as the binding target in Accessibility AC1 and restated in the Business Value section and Definition of Done. Correct, unqualified, no per-route carve-out.
- **Three named breakpoints matching Tailwind's `sm`/`md`/`lg` scale**: Responsive AC1 states mobile `<640px`, tablet `640–1024px`, desktop `≥1024px` — verified against Tailwind's actual scale (`sm` = 640px, `lg` = 1024px) and against the kickoff pass's own Section 3 wording: an exact match, not a paraphrase that drifted.
- **44×44px touch targets**: Responsive AC5, stated correctly, scoped to mobile/tablet only (not desktop's mouse-driven targets) exactly as the kickoff pass's own Section 3 constraint reads.
- **axe-core automated coverage**: Accessibility AC2, zero critical/serious violations at gate time, moderate/minor findings tracked as named backlog items rather than silently dropped or silently blocking — matches the kickoff pass's own framing verbatim.
- **The nine named e2e flows**: Accessibility AC4 lists authentication, dashboard, budget creation, transaction entry, account management, reports, goals, import, export — checked directly against `.claude/agents/e2e-test-engineer.md`'s own charter line ("Authentication / Dashboard / Budget creation / Transaction entry / Account management / Reports / Goals / Import flows / Export flows"), which is nine distinct bullets (Accessibility is the charter's tenth bullet, naming the *test category* this whole phase belongs to, not an additional flow to smoke-test). The spec's list matches all nine exactly, correctly folding both `/goals` and `/financial-goals` under the charter's single "Goals" bullet — no flow invented, none dropped.
- **The 14 accent-preset × theme contrast combinations**: Accessibility AC5 claims "6 presets (`blue`, `violet`, `emerald`, `amber`, `rose`, `teal`, per `src/app/globals.css`'s `[data-accent="..."]` rules) × 2 themes = 12 combinations, plus the no-accent-set default in both themes = 14 total." **Verified directly against `src/app/globals.css` for this pass, not taken on the spec's own word**: a grep for `data-accent` confirms exactly six `[data-accent="..."]` light-mode blocks and six matching `.dark [data-accent="..."]` blocks, named `blue`, `violet`, `emerald`, `amber`, `rose`, `teal` — the spec's count and naming are exactly correct, and the arithmetic (6×2+2=14) is correct.

**2. Scope compliance (Section 4) — confirmed, no drift.** Checked the spec against every one of the kickoff pass's six explicit exclusions: no new data model (the spec's own Open Question (a) resolution explicitly declines to add a `UserPreference` field, citing the kickoff pass's own out-of-scope list by name); no design-system overhaul (contrast fixes are explicitly scoped as "a targeted token adjustment... never... a removal of user choice," matching the kickoff pass's own "targeted token adjustment, not a rebrand" wording); no PWA/offline (not mentioned anywhere in the document); no i18n (not mentioned); no backend performance work (not mentioned — the spec's entire subject matter is presentation-layer); no second animation library (5a introduces no animation at all, correctly deferring all of it to 5b). **No drift found on any of the six.**

**3. The two Open Question resolutions — both sound, both agreed, no correction needed.**
- **(a) No persisted reduced-motion override in 5a/5b, OS-level only.** Agreed. This is exactly the outcome this kickoff pass's own Section 5 flagged as needing a scope call, and the spec's reasoning (no demonstrated need; OS-level `prefers-reduced-motion` is a complete, standard mechanism on its own terms, not a partial one; a persisted override is unbudgeted schema/product work for a phase explicitly scoped to add no new data model; the door remains open for a future, small, well-scoped feature if real demand ever appears) is the same "don't build the general/configurable version of a capability before a demonstrated need justifies it over the simpler, standard one" discipline this project has now applied consistently three times running — Risk #28's rejection of a drag-and-drop widget-builder and full multi-currency support at 4c's kickoff, and now this. Consequence for the Solution Architect pass is stated correctly: no Database Architect participation needed on this question's account.
- **(b) Bottom nav is additive/narrow (4–5 items), not a replacement for the hamburger `Sheet` sidebar.** Agreed. This resolves Risk #42 exactly as anticipated — the spec correctly identifies that a full-replacement bottom nav would either duplicate the sidebar's complete 24-route coverage (a direct "avoid duplication" violation) or, if narrowed to fit, would no longer be a full replacement at all, and that the two-tier "narrow bottom nav for daily destinations, full hamburger sidebar for everything else" structure is both the standard mobile pattern for an app this shape and the only version of "bottom navigation" that can satisfy this phase's own touch-target and no-clipped-content bars at once. The specific 4–5-route set is correctly left to the Frontend Lead's implementation call, consistent with this document's own stated boundary against designing component-level decisions.

Both resolutions are sound and require no correction — cleared as written.

**4. Route/screen inventory — spot-checked directly against source, confirmed complete and accurate.** Walked `src/app/(dashboard)/**`, `src/app/(auth)/**`, and `src/app/admin/**` directly via glob, and cross-checked every route against `components/shared/sidebar.tsx`'s `NAV_SECTIONS` (the canonical nav structure) rather than trusting the spec's own enumeration. Every one of the spec's 24 routes corresponds to an actual `page.tsx`; every actual `page.tsx` found by the direct filesystem walk corresponds to one of the spec's 24 routes (including every dynamic detail sub-route — `/transactions/[id]`, `/goals/[goalId]`, `/bills/[billId]`, `/income/[streamId]`, `/investments/[holdingId]`, `/financial-goals/[goalId]` — each correctly folded under its parent's inventory line rather than double-counted); `(auth)` contains only `/login` (no separate signup/forgot-password route exists, matching the spec's single "Unauthenticated" entry); no root `/settings` page exists (`settings/notifications`, `settings/appearance`, `settings/preferences` are the only three, matching the spec exactly). **No error found — the inventory is complete and accurate as written, and is safe for 5b's later spec to reuse verbatim as its own stated dependency requires.**

**5. Per-surface mobile-treatment decisions (Responsive AC3) — three of four already correct; two genuine gaps found by inspecting every other data-heavy surface directly, and closed by editing the spec.** The kickoff pass's own Section 3 names Transactions, Reports, Analytics charts, and Admin's user list/audit log as the data-heavy surfaces needing an explicit decision; the spec resolves all four soundly (Transactions/Admin: card-list fallback, justified by both being high-interaction-frequency `DataTable`-primitive tables with per-row actions that don't fit a squeezed desktop grid on a 375px screen; Reports: column-priority collapse, justified by it being an action-oriented form plus occasional preview table, not a high-frequency interactive table; Analytics: horizontal-scroll-with-affordance, justified by charts being shape-dependent visuals a card-list conversion would destroy). This pass then checked every other data-heavy surface named in the dispatch prompt, and one more the dispatch prompt didn't name, directly against source rather than assuming the spec's four-surface list was exhaustive:
   - **Debt's payoff-comparison view** (`features/debt/components/strategy-comparison.tsx`): already a `grid-cols-1 sm:grid-cols-2` two-panel layout that stacks to one column below `sm`, not a wide multi-column table — it already reflows correctly by construction and needs no additional AC3-style decision beyond the generic AC2 breakpoint verification every route gets.
   - **Investments' holdings list** (`features/investments/components/holding-row.tsx`): already a `flex flex-wrap` row layout, not a rigid `DataTable` grid — likewise already reflow-friendly by construction, no additional decision needed.
   - **Budgeting's category rows** (`features/budgeting/components/budget-category-row.tsx`): a plain row component, not a `DataTable` consumer — same conclusion.
   - **Bills' occurrence-history table and Recurring Income's occurrence-history table** (`features/bills/components/occurrence-history-table.tsx`, `features/recurring-income/components/occurrence-history-table.tsx`, on `/bills/[billId]` and `/income/[streamId]` respectively): **a genuine gap.** Both compose the exact same `components/shared/data-table` primitive as Transactions and Admin's `UserTable`/`AuditLogTable` — confirmed by a direct grep of every `components/shared/data-table` import in `src/`, which returns five consumers total, not the four the original spec's Dependencies section named (corrected from an earlier "six" miscount by the Phase 5a Release Manager's own re-grep during the first-pass review). Both have the same seven-column shape (due/expected date, status, paid/received amount, paid/received date, paid-via, on-time/late, a per-row Mark Paid/Received-or-Unmark action button) that already justified Transactions'/Admin's card-list decision. The original spec's AC3 simply never named them, and its Dependencies section under-counted `DataTable`'s own consumers as a direct symptom of the same miss.
   - **Calendar v2's month grid** (`features/calendar/components/calendar-grid.tsx`, `/calendar`): **a second genuine gap.** This is a seven-column, up-to-six-row month grid where a single day cell can hold multiple entries (bills, paydays, the budget-reset marker) — a data-dense surface by any reasonable reading of AC3's own "data-heavy surface" bar, and arguably the app's second-most information-dense screen after Transactions. None of the three patterns the spec already resolves (card-list, column-priority collapse, horizontal-scroll-with-affordance) is a natural fit for a fixed seven-day grid, and the original draft simply left it undecided, in tension with AC3's own explicit "decided once... not left open" requirement.

   **Action taken**: `docs/product/phase-5a-accessibility-responsive.md` was edited directly (the same standing authority the Phase 4b and 4c resolution passes exercised when they found and fixed a genuine spec gap or overreach, rather than sending it back to the Product Owner for a fixable, low-risk, mechanical correction). Two additions were made to Responsive AC3: (1) Bills'/Recurring Income's occurrence-history tables were added to the existing card-list-fallback bucket, reusing the identical decision and reasoning already established for Transactions/Admin rather than inventing a new one — a mechanical, low-risk extension, not a new design call; (2) Calendar v2's month grid was given its own, fourth, explicitly-reasoned pattern — a condensed grid (date number plus a small non-color-reliant per-entry-type indicator) below tablet with a tap-to-expand day-detail view, preserving every entry's existing link to its own Bill/Income-stream detail page — with the exact day-detail affordance's component boundary correctly left to the Solution Architect's pass, consistent with this document's own stated boundary against designing component-level decisions itself. The Dependencies section's `DataTable`-consumer list and the Definition of Done's Responsive bullets were updated to match. See both new risk-register rows below.

**6. New scope questions, gaps, or risks surfaced by this pass, beyond the two already closed above — none requiring further Product Owner rework.** No other cross-doc inconsistency was found between this spec and any already-shipped product doc it cites (`calendar-v2.md` AC5's "color is never the sole distinguishing signal" is correctly restated in Accessibility AC8; `calendar-and-notifications.md` AC4's "selecting a calendar entry takes the user to that bill's detail" is correctly preserved, unchanged, by the new Calendar v2 mobile-treatment decision above). No new risk beyond the two mobile-treatment gaps (now closed) was found in this pass's review — Risks #39–45 from the kickoff pass all remain accurately described by the now-corrected spec, and no further extension to any of them is needed as a result of this review.

**Conclusion: the spec honors every one of the kickoff pass's six binding constraints, exactly as written and re-verified against source; it introduces no scope drift against Section 4's exclusions; both Open Question resolutions are sound and require no correction; the 24-route inventory is complete and accurate; and the one real gap this pass found (two data-heavy surfaces missing an explicit mobile-treatment decision) has been corrected directly in the spec, not merely flagged.** `docs/product/phase-5a-accessibility-responsive.md` is **cleared for the Solution Architect pass** (milestone 3 of Phase 5a's own milestone list above) — no further Product Owner rework is required before that pass begins.

---

## Immediate next step

Phase 4 is complete — 4a, 4b, and 4c have all shipped and passed their review gates (see `docs/release/phase-4c-notes.md`/`phase-4c-checklist.md` for 4c's final APPROVE, no open follow-up items). The Phase 5 CTO kickoff pass split Phase 5 into Phase 5a (Accessibility & Responsive Foundation) and Phase 5b (Motion & Craft), sequenced so 5a's structural work lands before 5b's decoration work, named concrete acceptance criteria for all three workstreams (accessibility, responsive, motion), and flagged two open questions (reduced-motion persistence scope, bottom-nav-vs-hamburger-sidebar relationship) for the Product Owner's spec to resolve. The Product Owner's Phase 5a spec (`docs/product/phase-5a-accessibility-responsive.md`) has since been produced, resolving both open questions, and **the CTO resolution pass above is now done**: every kickoff-pass binding constraint was re-verified against source and confirmed honored, no scope drift was found, both Open Question resolutions were confirmed sound, the 24-route inventory was spot-checked and confirmed accurate, and the one real gap found (two data-heavy surfaces — Bills'/Recurring Income's occurrence-history tables, and Calendar v2's month grid — missing an explicit mobile-treatment decision) was corrected directly in the spec. Two new risk-register rows (#46–#47) were added to track that finding.

Dispatch the **Solution Architect pass for Phase 5a** next (milestone 3 of the Phase 5a milestone list above) — it decides the Playwright/axe-core bootstrap approach, designs the bottom-navigation component's boundary/reuse relationship to `components/shared/sidebar.tsx`, designs the Calendar v2 day-detail affordance the resolution pass above left to this pass, and decides on visual-regression tooling. No Database Architect participation is needed (the reduced-motion-override question resolved to "no, OS-level only"). Phase 5b's own Product Owner spec pass does not begin until 5a's review gate passes, per this roadmap's own phase-gate rule.

**Phase 5a is now complete.** It ran the full pipeline: CTO kickoff → this Product Owner spec/resolution → Solution Architect (`docs/architecture/phase-5a-technical-design.md`) → parallel UI Component Engineer/E2E infrastructure build → Frontend Lead implementation → an accessibility structural-fix pass (which found and fixed a real regression plus a wider-than-scoped contrast sweep) → E2E re-verification to a clean 32/32 axe-core gate → 9 flow tests (finding and fixing one genuine race-condition bug along the way) → the full review gate (Security Architect APPROVE, Performance Engineer APPROVE, Bug Hunter: 2 High + 1 Medium fixed, 1 Medium accepted as tracked debt) → Release Manager first pass **REJECT** on one blocking finding (5 of 6 accent-color presets failing WCAG 2.1 AA contrast, per `docs/release/phase-5a-notes.md`) → the blocking fix (real WCAG relative-luminance math, not assumed shade folklore, plus a new parametrized `tests/e2e/accessibility/accent-contrast.spec.ts` regression test covering all 6 presets) and two secondary fixes (a second Decimal-leak instance in `goals/server/service.ts`, and a "six"/"five" consumer-count doc miscount) → Release Manager second pass **APPROVE** (`docs/release/phase-5a-second-pass.md`), closing Phase 5a with no further open items beyond one residual non-blocking documentation line (noted in the second pass, not gating).

**Dispatch the Product Owner for Phase 5b (Motion & Craft) next** — per the Phase 5 CTO kickoff pass's own stated build order, 5b's spec is grounded in 5a's now-final, now-audited structure (the accessibility/responsive DOM shape animation will be built against). This is the next phase-gate step in the pipeline: CTO resolution → Solution Architect → implementation (UI Component Engineer builds reusable animation primitives first, then Frontend Lead applies them) → full review gate → Release Manager sign-off.

---

## Phase 5b CTO resolution pass (2026-08-03): four of five capabilities cleared as written; Number Counters' scope/mechanism definition sent back to the Product Owner; one chart-count miscount fixed directly

Supersedes the "Immediate next step" section's own "Dispatch the Product Owner for Phase 5b (Motion & Craft) next" pointer above: that dispatch happened, and `docs/product/phase-5b-motion-craft.md` has been delivered. Per this phase's own milestone 2 (and every prior phase's identical step — 3a/3b's resolution pass, 4a's/4b's/4c's resolution passes, 5a's own resolution pass immediately above) — read the Phase 5b Product Owner spec in full against Section 3's ("Motion & Craft (5b)") binding constraints and Section 4's out-of-scope list before authorizing the Solution Architect pass (Section 6 milestone 3). As with every prior resolution pass, this was a line-by-line check against the kickoff pass's own binding wording, plus direct re-verification of the spec's own factual claims against the actual codebase — not trust taken on the spec's prose alone — with extra weight on the claims most load-bearing for the Solution Architect's upcoming primitive-design work (the chart-consumer list, the `DataTableCardList`-consumer count, the "no `Collapsible`/`Accordion` exists" claim, and the Number Counters capability's own claimed surface list).

### 1. Binding-constraint compliance (Section 3's "Motion & Craft (5b)") — confirmed, all constraints honored, with reasonable, well-justified concretizations where the kickoff pass left a number unfixed

- **`prefers-reduced-motion` as a binding, centrally-implemented requirement**: Reduced-Motion Foundation AC1 states this almost verbatim ("implemented once, centrally... never a per-component ad hoc check"), and correctly folds in the two pre-existing motion instances (`progress-ring.tsx`, `progress.tsx`) under the same mechanism (AC3) rather than treating them as already-exempt. Matches the kickoff pass's wording and Risk #40's own framing exactly.
- **Number counters' duration bound**: the kickoff pass said "roughly 300–600ms"; the spec states a firm "300–600ms" (AC2). Tightening a hedge ("roughly") into a fixed, testable bound is exactly the "flesh into full acceptance criteria" role this spec is supposed to play (the same role 4a's spec played for the CTO's structured-output constraints) — not a reinterpretation or a softening. Accepted as written.
- **Number counters routed through the same `formatCurrency`/`useFormatCurrency` pipeline, never a second formatting path**: AC3 restates this as the capability's single most emphasized constraint, correctly tied back to 4c's currency-display fix. Sound as a requirement — see Section 5 below for a real gap this pass found in how that requirement interacts with `StatCard`'s existing prop contract.
- **Chart transitions bounded, consistent, CLS-free, native-Recharts-mechanism-first**: AC1–AC3 match the kickoff pass's wording exactly, including the tool-fit discipline (Recharts' own animation props for Recharts charts, Framer Motion reserved for the one non-Recharts visualization — the heatmap — that has no native prop to lean on). The 400–800ms figure is this spec's own supplied number (the kickoff pass fixed a number for counters but not for charts); the reasoning given (denser visual event than one number, but shorter than Recharts' own 800–1500ms defaults, consistent with the Business Value section's own "smooth, not slow" framing) is sound and internally consistent — no correction needed.
- **Page transitions: no TTI regression, no interference with `loading.tsx`, `(dashboard)`-route-group-only scope**: AC1–AC4 match the kickoff pass's wording exactly, including the explicit `/login` and Admin exclusions (both correctly reasoned, not just asserted).
- **Expandable cards: one reusable primitive, correct `aria-expanded`/`aria-controls`, Framer-Motion-orchestrated reveal**: AC1–AC3 match the kickoff pass's wording exactly. The claim that no `Collapsible`/`Accordion` primitive exists anywhere in `src/` today was re-verified directly by this pass (grep of `Collapsible|Accordion` across `src/`: zero matches) — accurate, not asserted on the spec's own word.
- **Framer Motion remains the sole animation library; CSS/Tailwind remains the default for simple hover/focus state changes**: restated correctly in the Definition of Done and the Cross-Cutting section. `package.json` confirms `framer-motion@^12.42.2` as stated, with no second animation library added anywhere in the spec's Dependencies section.
- **GPU-compositable properties preferred, with named exceptions**: the Cross-Cutting section's two named exceptions (expandable-card height reveal, `ProgressRing`'s pre-existing `strokeDashoffset`) were checked directly against the actual implementations — `components/ui/progress.tsx`'s indicator uses `style={{ transform: \`translateX(-${100 - clamped}%)\` }}` (a true `transform`, matching the spec's own characterization) and `components/shared/progress-ring.tsx`'s `motion.circle` animates `strokeDashoffset` via Framer Motion exactly as described (`initial`/`animate`/`transition` on that one property, `duration: 0.6`). Both claims are accurate.
- **Systematic pass across the full route inventory, not a per-feature bolt-on**: each capability's AC explicitly states which routes it applies to and, just as importantly, which it excludes and why (Number Counters AC7, Chart Transitions AC6, Page Transitions AC4) — matches the kickoff pass's own framing.

### 2. Scope compliance (Section 4) — confirmed, no drift

Checked against all six of the kickoff pass's explicit exclusions: no new data model (Reduced-Motion Foundation AC2 explicitly restates 5a's "no persisted override" resolution as settled, not re-litigated); no design-system overhaul (nothing in this spec touches color tokens, typography, or component visual style — it is exclusively about motion timing/mechanism); no PWA/offline (not mentioned); no i18n (not mentioned); no backend/database performance work (the spec's entire subject is client-side presentation; the one persistence-adjacent item, a report-generation-event log, belongs to 4c and isn't touched here); no second animation library (confirmed above). **No drift found on any of the six.**

### 3. Route/screen inventory — reuse verbatim, confirmed accurate by direct comparison against the 5a source document

Walked `docs/product/phase-5a-accessibility-responsive.md`'s own Route/Screen Inventory line by line against this spec's copy: every one of the 24 routes, its numbering, and its section grouping (Unauthenticated / primary / Planning / Wealth / Account / Admin) match exactly. The one addition — `BottomNav (new in 5a)` appended to the Global Chrome line — is a legitimate, transparently-labeled update reflecting a component that didn't exist when 5a's own inventory was first drafted, not silent drift; it does not touch the 24-route count or grouping, which is what "reused verbatim" is actually claiming. **No error found; the claim holds.**

### 4. The four sub-ambiguity resolutions (kickoff pass Section 3's unfixed numbers/boundaries) — three sound as written; the fourth (Number Counters' scope) has a real, load-bearing gap, addressed in Section 5 below

- **Chart-transition duration (400–800ms)**: reasoning is sound (Section 1 above). No correction needed.
- **Expandable cards' concrete consumer scope (the one ad hoc Analytics instance + all five `DataTableCardList` consumers)**: re-verified directly, not taken on the spec's word. A grep for `ResponsiveDataTable`/`DataTableCardList` usage across `src/` (excluding the primitive's own definition files and two incidental comment-only mentions in `calendar-grid.tsx` and `table.tsx`) finds exactly five real consumers: `transaction-table.tsx`, `admin/components/user-table.tsx`, `admin/components/audit-log-table.tsx`, `bills/components/occurrence-history-table.tsx`, and `recurring-income/components/occurrence-history-table.tsx`. This spec correctly picked up 5a's own corrected count (five, per Risk #46) rather than the stale "six" an earlier 5a draft briefly carried — exactly the discipline this resolution pass exists to check for. `subscriptions-list.tsx`'s ad hoc toggle was independently re-verified: it sets `aria-expanded` but no `aria-controls` anywhere in that file, matching the spec's characterization exactly.
- **Page transitions' route-tree scope (`(dashboard)` group only)**: matches the kickoff pass's Section 3 wording with no expansion or contraction.
- **Number counters' in/out-of-scope surfaces**: this is where a real problem was found — see Section 5.

### 5. Headline finding — Number Counters' AC1 (mechanism) and AC6 (surface list) contradict each other against the actual codebase; six of ten named surfaces don't use the mechanism AC1 says defines this capability's entire scope

AC1 defines this capability's applicability strictly by component: "every headline currency or percentage figure rendered through `components/shared/stat-card.tsx`'s `value` prop, or through `components/shared/progress-ring.tsx`'s default percentage label." AC6 then names ten features as this capability's "concrete in-scope surfaces... every route/component rendering a `StatCard` or `ProgressRing` headline figure": Dashboard, Accounts, Budgeting, Goals/Financial Goals, Bills, Recurring Income, Debt, Investments, Analytics, and Financial Health Score detail.

Checked directly against source, feature by feature, rather than trusted from the spec's own enumeration:

- **Dashboard** (`dashboard-card-groups.tsx`), **Budgeting** (`budget-summary-cards.tsx`, `budget-health-score-badge.tsx`), **Goals**/**Financial Goals** (`goal-card.tsx`, `goals/[goalId]/page.tsx`, `financial-goal-card.tsx`), and **Recurring Income** (`expected-upcoming-income-card.tsx`) — all four genuinely do render through `StatCard` and/or `ProgressRing`, confirmed by direct grep. AC6 is correct for these four.
- **Accounts**: `features/accounts/components/account-card.tsx` renders its balance as a plain `<span className="font-heading text-2xl font-semibold">{formatCurrency(account.balance)}</span>` inside an ordinary `Card`/`CardContent` — no `StatCard` import anywhere in `features/accounts/`.
- **Debt**: `features/debt/components/debt-card.tsx` renders its balance the identical way — `<span>{formatCurrency(debt.effectiveBalance)}</span>` — no `StatCard`/`ProgressRing` import anywhere in `features/debt/`.
- **Investments**: not even its own dedicated "headline" summary component, `features/investments/components/portfolio-overview-section.tsx` (total current value, total dividend income — precisely the kind of figure this capability is meant to cover), uses `StatCard`; it hand-rolls `Card`/`CardHeader`/`CardContent` with a plain `<span>` and a directly-imported `formatCurrency`. No `StatCard`/`ProgressRing` import anywhere in `features/investments/`.
- **Analytics**: every currency-rendering component (`top-merchants-list.tsx`, `largest-purchases-list.tsx`, `budget-vs-actual-table.tsx`, etc.) calls `formatCurrency` directly. No `StatCard`/`ProgressRing` import anywhere in `features/analytics/`.
- **Financial Health Score**: the Dashboard summary badge (`financial-health-score-badge.tsx`) renders the score as a bare `<span>{breakdown.score}</span>`; the detail-route breakdown grid (`financial-health-score-breakdown.tsx`) renders all four subscores the identical way, each in a plain `<div>`/`<span>` pair. Neither imports `StatCard` or `ProgressRing`; no match anywhere else in `features/financial-health-score/` either.

Six of AC6's ten named surfaces, in other words, do not use the mechanism AC1 says defines this capability's scope. This is not a minor miscount like the chart-count issue in Section 6 below — it's an internal contradiction between the capability's own stated mechanism and its own stated surface list, and it is genuinely load-bearing: an implementer following AC1 literally would correctly conclude these six surfaces are simply out of scope (no `StatCard`/`ProgressRing` to hook the counter into), silently cutting six of ten promised surfaces with nobody having decided to; an implementer instead trying to honor AC6's promise would have to invent a second, ad hoc per-consumer counting mechanism for plain `formatCurrency`-driven spans — directly contradicting this same capability's own "one consistent counting treatment... not tuned per screen or per consumer" bar (AC2) and the Cross-Cutting section's "no exception without one being named here" rule.

**This is not a mechanical fix this pass can make unilaterally.** At least three different, equally defensible resolutions exist, and choosing between them is a real product/scope call, not an obvious correction:
(a) broaden AC1's own mechanism definition to cover any headline currency/percentage figure rendered through `formatCurrency`/`useFormatCurrency`, regardless of which shared component (or none) renders it — the reading most consistent with the User Story's every-number framing, but one that materially changes the technical shape of the counter primitive the Solution Architect is about to design (an integration surface broader than "wrap `StatCard`/`ProgressRing`");
(b) narrow AC6 to the four surfaces that actually qualify today (Dashboard, Budgeting, Goals/Financial Goals, Recurring Income), explicitly demoting Accounts/Bills/Debt/Investments/Analytics/Financial Health Score to a named, tracked follow-up — a legitimate, much smaller-scope call, but one that cuts roughly 60% of this capability's claimed surface without anyone having decided to; or
(c) some hybrid — e.g., migrating some or all of the six affected surfaces' headline figures onto `StatCard` as part of this phase, a modest but real component-level change this phase's own "no design-system overhaul" exclusion doesn't obviously forbid (a component swap, not a redesign) but doesn't obviously authorize either.

**Action taken: sent back to the Product Owner, not resolved here.** `docs/product/phase-5b-motion-craft.md`'s Number Counters AC1 and AC6 need to be reconciled with each other and with the actual codebase before the Solution Architect's shared-counter-primitive design work begins — the primitive's own integration boundary depends directly on which of the above this spec ultimately commits to. New Risk #53 tracks this as the blocking item. A brief inline flag has been added to AC6 and the Open Questions Resolved section of `docs/product/phase-5b-motion-craft.md` pointing back to this finding, so an implementing agent reading that document alone isn't misled by the unedited AC1/AC6 text — the substantive resolution is the Product Owner's to make, not pre-empted here.

### 6. A genuine but low-risk, mechanical miscount — found and fixed directly

Chart Transitions AC4 correctly enumerates fourteen named Recharts chart components (Dashboard's four, Analytics' six, Investments' two, Financial Goals' one, Financial Health Score's one) — re-verified directly against a grep of every `recharts` import in `src/features/`, which returns exactly fourteen files, an exact match to AC4's own list by name. The Dependencies section, however, separately describes the same set as "used across all twelve chart components" — an internal miscount against the spec's own correct enumeration two sections earlier, not a fact the codebase disagrees with (AC4 already had it right). **Fixed directly** — `docs/product/phase-5b-motion-craft.md`'s Dependencies section now reads "fourteen," matching AC4 and the source. This is the same class of fixable, low-risk, mechanical correction the Phase 5a resolution pass made for the "six"/"five" `DataTableCardList`-consumer miscount (Risk #46) — the CTO's own standing authority to make this kind of fix directly, rather than sending an obviously-correct one-line count back for a full Product Owner re-dispatch. New Risk #54 records it.

### 7. A secondary architecture flag, not a spec defect — `StatCard`'s existing prop contract

Independent of the Section 5 finding, this pass noticed that every current real call site of `StatCard`'s `value` prop (`budget-summary-cards.tsx`, `expected-upcoming-income-card.tsx`, `dashboard-card-groups.tsx`, `goal-card.tsx`, `financial-goal-card.tsx`) passes an already-formatted string — `value={formatCurrency(x, currency)}` — matching the component's own documented contract ("Already-formatted value (this component performs no number formatting)"). Number Counters AC1/AC3 require the animated, in-flight value to be numerically interpolated and re-formatted on every tick through that same pipeline, which needs the raw number and currency, not a pre-formatted string. This is a real design fork the Solution Architect will hit immediately, but it is squarely the primitive's own implementation-boundary question this spec correctly and explicitly defers ("it does not design... the expandable-card primitive's file/prop boundary... those are the Solution Architect's and UI Component Engineer's calls") — not a Product Owner spec defect, and not something this pass is correcting. Flagged for the Solution Architect's attention as new Risk #55, the same "flag now so it isn't discovered mid-architecture" treatment Risk #44/#45 already received at Phase 5 kickoff.

### 8. Business Value / User Story — checked for overclaim, none found

The Business Value section correctly disclaims the roadmap stub's own "software someone would pay for" language as unverifiable prose (mirroring 5a's identical move), replacing it with four concrete, measurable proxies in Success Metrics. It correctly frames this phase as the "payoff" for 5a's structural investment rather than a parallel workstream, and correctly elevates `prefers-reduced-motion` to a hard requirement rather than optional polish, consistent with the kickoff pass's own framing. No scope creep or overclaim found in either section.

### Conclusion

Four of this spec's five capabilities (Reduced-Motion Foundation, Chart Transitions, Page Transitions, Expandable Cards) honor every one of the kickoff pass's binding constraints, introduce no scope drift against Section 4's exclusions, and are cleared for the Solution Architect pass as written. The fifth (Number Counters) has a genuine, load-bearing internal contradiction between its own mechanism definition (AC1) and its own surface list (AC6) — six of ten named surfaces don't use the mechanism the capability claims defines its scope — and is **sent back to the Product Owner** for a specific, named fix (Section 5 above) before the Solution Architect's counter-primitive design work begins. One low-risk, mechanical miscount (the chart-component count) was found and fixed directly. `docs/product/phase-5b-motion-craft.md`'s Dependencies section has been corrected; its Number Counters capability awaits the Product Owner's revision. New risk-register rows #53–#55 track all three findings.

**The Solution Architect pass may begin now for Reduced-Motion Foundation, Chart Transitions, Page Transitions, Expandable Cards, and the Cross-Cutting GPU-Compositable-Properties Bar. It should not scope the shared counter primitive's design (Number Counters) until the Product Owner's revised AC1/AC6 land** — a narrower, targeted re-check of that one capability, not a full spec re-review, is all that's needed once the revision arrives, the same fast reject/fix/re-check cycle 5a's own Release Manager pass used for its accent-contrast finding.

### 9. Follow-up re-check (2026-08-03): Number Counters revision confirmed sound — full Phase 5b spec now cleared for the Solution Architect pass

The Product Owner has revised `docs/product/phase-5b-motion-craft.md`'s Number Counters capability. Per this section's own closing instruction above ("a narrower, targeted re-check of that one capability, not a full spec re-review, is all that's needed once the revision arrives"), this is that re-check — scoped only to Number Counters (AC1–AC7, its Edge Cases, and Open Questions Resolved item 3), not a re-review of the other four capabilities or the Cross-Cutting bar, which stay cleared as they were in the pass above.

**AC1/AC6 contradiction: resolved, verified by direct re-read.** AC1 now defines this capability's mechanism as "every headline currency or percentage figure produced through the `formatCurrency`/`useFormatCurrency` pipeline (Phase 4c) — application-wide, regardless of which shared component, if any, renders it," explicitly naming `StatCard`'s `value` prop and `ProgressRing`'s label as two existing renderers among others, not the exhaustive definition. AC6's ten-surface list is unchanged and is now correctly grounded by that broadened definition — the surface list and the mechanism definition agree with each other and with the actual codebase (the same six surfaces this pass's own Section 5 confirmed render via plain `formatCurrency`-driven spans are now correctly in scope by the pipeline they use, not excluded for lack of a `StatCard` wrapper). **This is resolution (a)** of the three options this pass named on 2026-08-03 — broadening the mechanism definition, not narrowing AC6 (option b) or mandating a `StatCard` migration (option c).

**Internal consistency with this capability's own other ACs — confirmed, no scope creep found.** The broadened AC1 explicitly retains the "headline, not row-level" gate from AC7 ("provided it clears the... bar this capability's own AC7 already draws"), so the broadening extends *which component* can carry an in-scope figure, not *which figures* are in scope — Transactions' row-level amounts, Reports' static PDF output, and Admin's absent financial figures remain excluded exactly as before, and the spec says so explicitly ("No new AC7 exclusion is created by this broadening... none of them fail AC7's exclusion for lack of a `StatCard`/`ProgressRing` wrapper in the first place"). AC2's duration bound (300–600ms, one shared value) and AC4's "percentage figures get identical treatment" are both restated unchanged and are not in tension with a broader AC1 — if anything, a per-component mechanism would have been the one at risk of forcing a second, tuned-per-consumer behavior (a live AC2 violation for exactly the reason Section 5 flagged), which the broadened, pipeline-based definition avoids by design. AC3 (the in-flight value rendered through the same `formatCurrency`/`useFormatCurrency` pipeline) is what the broadened AC1 is defined *by*, not something it strains against. No new exclusion, no new exception, no quiet expansion beyond "every headline figure, not per-row" — the scope is the same ten surfaces this pass already reviewed, now correctly reachable under one non-contradictory rule.

**Reasoning for rejecting (b) and (c) — sound, and correctly deferential to the Solution Architect/UI Component Engineer.** The revision's own reasoning (Open Questions Resolved, item 3) rejects (b) narrowing on the grounds that cutting six of ten already-promised, prominent surfaces (Accounts, Debt, Investments, Analytics, both Financial Health Score surfaces) would be a real, user-visible regression against the User Story's own "the numbers... I interact with every day" framing, not a neutral scope trim — a sound call given this document's own opening framing of the capability. It implicitly rejects (c) mandating a `StatCard` migration by stating plainly that "which component (or new primitive) actually renders each of the ten surfaces' figures, and whether any of the six affected surfaces are migrated onto `StatCard`... is deliberately left to the Solution Architect and UI Component Engineer" — correctly treating the rendering-component question as an implementation-boundary call, consistent with this same Product Owner spec's own stated opening boundary against designing "the counter primitive's integration boundary" itself. This is the right place to draw that line: the spec commits to *what* must animate (a product-scope decision, squarely the Product Owner's to make) and leaves *how* it is rendered to the Solution Architect/UI Component Engineer, exactly as this document's introduction already promises for every other capability's component-level decisions.

**Downstream edits — consistent, no new claim introduced.** The Definition of Done bullet ("Number counters verified across every in-scope surface named in that capability's AC6 — all ten surfaces, regardless of which component renders each one's headline figure today... within the 300–600ms bound") restates AC1/AC6's now-consistent scope without adding anything beyond what those ACs already commit to. The Dependencies section's `formatCurrency`/`useFormatCurrency` bullet, now also describing itself as "the mechanism by which Number Counters' scope is now defined," is accurate and points back to the same Open Questions Resolved item 3 rather than asserting anything new. Neither edit needs separate verification beyond what's already covered above.

**This pass's own inline flags — confirmed removed, not left dangling.** A direct re-read of the current AC6 surface list and the Open Questions Resolved item 3 rewrite finds clean, finished prose with no bracketed flag markers, no "pending Product Owner" placeholder text, and no reference to this being an open or unresolved item — a grep of the current spec for flag-adjacent language (`flag`, `pending`, `TBD`, `contradiction`, `sent back`) turns up nothing left dangling alongside the new text. The inline flags this pass added on 2026-08-03 have been fully superseded by the revision, not merely supplemented by it.

**No new problem introduced.** The revision does not touch the other four capabilities or the Cross-Cutting bar (confirmed by re-reading the full document — Reduced-Motion Foundation, Chart Transitions, Page Transitions, and Expandable Cards are byte-for-byte the same sections already cleared above), and it does not reopen Risk #55 (the `StatCard` pre-formatted-string prop-contract question) — the revision explicitly says so, and correctly, since that remains a Solution Architect implementation-boundary question this product-level scope decision was never trying to answer.

**Conclusion: Number Counters is cleared.** Risk #53 is closed. **The full Phase 5b spec — all five capabilities (Reduced-Motion Foundation, Chart Transitions, Page Transitions, Expandable Cards, Number Counters) plus the Cross-Cutting GPU-Compositable-Properties Bar — is now cleared for the Solution Architect pass**, including the shared counter primitive's design, which this pass's earlier note above had held back pending this revision. Risk #54 (chart-count miscount, already closed) and Risk #55 (StatCard's prop-contract question, still open and correctly routed to the Solution Architect) are unaffected by this follow-up.

---
