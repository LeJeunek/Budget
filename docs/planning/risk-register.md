# FinanceOS — Risk Register

| # | Risk | Phase | Likelihood | Impact | Mitigation | Owner |
|---|------|-------|-----------|--------|------------|-------|
| 1 | Transaction/Account schema designed too narrowly in Phase 1, forcing breaking migrations when Debt/Investments (Phase 3a) need to reuse "account-like" concepts | 1→3a | High | High | Database Architect modeled `Account` with a `type` discriminator and shared balance/interest fields from the start. Resolved at 3a kickoff: Database Architect decided and documented the Debt/Investment linkage approach in `docs/database/` before 3a backend work began. **Resolved** — 3a shipped and passed its review gate. | Database Architect |
| 2 | AI transaction categorization / insights are vulnerable to prompt injection via user-controlled text (merchant names, transaction notes) | 4a | Medium | Medium | Treat all transaction text as untrusted input to the model; structured-output schema validation (Zod) on every AI response before it touches the database; Security Architect review required before release. **Extended for 4a kickoff (2026-07-21):** this is the first AI/LLM integration this codebase has ever had, so the mitigation now explicitly includes a *design-stage* Security Architect review (before backend implementation starts), in addition to the standing pre-release review — see `roadmap.md`'s Phase 4a milestones. **Resolved** — 4a shipped and passed its review gate (design-stage and pre-release Security Architect review both APPROVE — `docs/release/phase-4a-notes.md`). | AI Engineer, Security Architect |
| 3 | CSV import (Phase 1) is a common source of malformed data, encoding issues, and duplicate-transaction bugs | 1 | High | Medium | Backend Engineer validates and dedupes on import; Bug Hunter specifically stress-tests large/malformed CSVs before Phase 1 sign-off. **Resolved** — Phase 1 shipped and passed its review gate. | Backend Engineer, Bug Hunter |
| 4 | Financial data (balances, transactions) is high-sensitivity; any authorization bug exposes one user's finances to another | All | Low | Critical | Every API route/Server Action scoped by authenticated user ID at the query level (never trust client-supplied user/account IDs alone); Security Architect reviews every phase before release, not just at the end. **Extended for Phase 4b (2026-07-21):** this same discipline now explicitly covers PDF report generation/download (a user must never be able to generate or fetch another user's report) and outbound notification email content — see Risk #17. **Extended for Phase 4c:** now also covers admin-side data views (view users, audit logs) — see Risk #18. | Backend Engineer, Security Architect |
| 5 | PDF report generation (Phase 4) and large transaction tables (Phase 1+) risk becoming a performance/memory bottleneck as data grows | 1, 4b | Medium | Medium | Pagination and server-side filtering from day one in Phase 1 (not retrofitted); Performance Engineer reviews before each phase's release. This concern is concretely realized by Phase 3b's Analytics suite (see Risk #11) and again by Phase 4b's PDF generation. **Extended for 4b kickoff (2026-07-21):** the deployment target is serverless (Vercel, per Phase 0), which constrains PDF-library choice — headless-browser-based rendering carries cold-start, function-size, and execution-time limits in that environment that a non-browser JS-native renderer would not. Flagged as a binding constraint for the Solution Architect's 4b library choice, not just a post-hoc performance review item — see `roadmap.md`'s Phase 4b section. **Extended for 4b kickoff (2026-07-27):** 4a's cron routes (`monthly-summary`, `financial-health-score-snapshot`, the auto-categorization batch job) are now live, proven examples of scheduled background work succeeding on this same Vercel target — a concrete, already-working fallback shape (pre-generate reports on a schedule rather than render synchronously on request) if the Solution Architect's chosen renderer still can't meet Vercel's execution-time limit for on-demand generation. Context for that choice, not a new constraint — see `roadmap.md`'s Phase 4b section. | Performance Engineer, Solution Architect |
| 6 | Feature scope creep — roadmap is already large; pressure to pull later-phase features into an earlier phase for a "richer demo" | All | High | High | Roadmap phase boundaries are binding; CTO approval required to move any feature to an earlier phase or across the 3a/3b boundary (see Risk #13) or the 4a/4b/4c boundaries (see Risk #16) | CTO |
| 7 | Better Auth is a comparatively newer library vs NextAuth; ecosystem/adapter gaps could block Phase 0 | 0 | Low | Medium | Time-boxed Phase 0 spike; fall back to NextAuth if Better Auth blocks progress for more than 1 day. **Resolved** — Better Auth shipped cleanly in Phase 0, no fallback needed. | Backend Engineer |
| 8 | Timezone/date handling bugs in bills, budgets (monthly resets), and recurring schedules | 2 | Medium | Medium | Store all dates in UTC, compute "due today"/"month boundary" logic against the user's stored timezone preference, not server time; explicit Bug Hunter pass on this before Phase 2 sign-off. **Resolved** — Phase 2 shipped and passed its review gate. Note for Phase 4c: the Customization spec's "themes/currency display" work should not silently introduce a second timezone-preference source — Solution Architect should confirm the existing preference storage (from Phase 2) is reused, not duplicated. | Backend Engineer, Bug Hunter |
| 9 | Debt/Investments schema shape (standalone models vs. extending the existing `Account` type discriminator, vs. a hybrid) decided hastily could force a breaking migration later once 3b's Net Worth aggregation and Analytics depend on it | 3a | Medium | High | Explicit Database Architect design decision as its own reviewed milestone before any Phase 3a backend work starts (not folded silently into implementation) — document the chosen approach and rationale in `docs/database/` before backend implementation begins. **Resolved** — decided and documented before 3a backend work began; 3a and 3b both shipped successfully on top of it. | Database Architect |
| 10 | Net Worth History chart (3b) has no historical data to chart if periodic snapshotting only begins when the chart UI itself is built | 3a→3b | High | Medium | Begin a lightweight periodic Net Worth Snapshot capture (backend only, no UI) at the end of Phase 3a — see `roadmap.md`'s Phase 3a milestones — so 3b's chart launches with real historical data already accumulated instead of an empty state. **Resolved** — shipped as scoped; the same "don't build the empty-history feature first" reasoning is reused for Phase 4c's Admin audit log (see Risk #16's split rationale in `roadmap.md`), and again for 4a's Financial Health Score snapshot job. | Backend Engineer |
| 11 | Analytics suite (3b) is compute-heavy over full Transaction/Account/Debt/Investment history (yearly spend, heatmaps, growth trends) — the general concern already flagged in Risk #5, now realized in concrete form | 3b | Medium | Medium | Performance Engineer reviews query/aggregation design before backend implementation begins (not only at phase-end release review); Solution Architect evaluates materialized/cached aggregate tables during the 3b architecture pass if live-query performance is a concern at realistic data volumes. **Resolved** — 3b shipped and passed its review gate. | Performance Engineer, Solution Architect |
| 12 | Financial Goals (broad, 3b) and Savings Goals (Phase 2) are conceptually adjacent — risk of user-facing duplication/confusion or two competing "goal" UIs if the boundary isn't explicit | 3b | Medium | Medium | Product Owner must define the explicit boundary and relationship between the two models in the 3b spec, before Database Architect schema work begins for `FinancialGoal`. **Resolved** — see `docs/product/financial-goals.md`'s explicit boundary decision; 3b shipped on top of it. | Product Owner |
| 13 | Splitting Phase 3 into 3a/3b creates a temptation to blur the boundary (e.g., pulling an Analytics metric into 3a "since it's related," or a Debt/Investment field into 3b) | 3a, 3b | Medium | Medium | The 3a/3b boundary defined in `roadmap.md` is binding, same as Risk #6; CTO approval required to move any feature across it. **Resolved** — no cross-boundary moves occurred; v1 shipped with the boundary intact. | CTO |
| 14 | Financial Health Score (Phase 4a) scope is ambiguous — an LLM-computed judgment vs. a deterministic formula over existing Analytics/Debt/Budget data with an optional AI-generated narrative — and guessing wrong misassigns module ownership (AI Engineer vs. Backend Engineer) before schema/architecture work | 4a | Medium | Medium | Product Owner must resolve this explicitly in the 4a spec, before the Solution Architect + AI Engineer architecture pass — see `roadmap.md`'s Phase 4a open scope question, following the same discipline Risk #12 established for the SavingsGoal/FinancialGoal boundary. **Resolved** — `docs/product/ai-features.md` resolved this as a deterministic formula with an optional AI narrative layer, CTO-approved weighting/bands (2026-07-22); 4a shipped and passed its review gate on top of it. | Product Owner |
| 15 | LLM/AI provider selection (Phase 4a) carries data-privacy (transaction/merchant text sent to a third party), cost, latency, and vendor-lock-in dimensions — this codebase has no existing AI/LLM integration to build on, so there is no established pattern to reuse or fall back to | 4a | Medium | High | Solution Architect + AI Engineer make this decision jointly during 4a's architecture pass, bound by CTO-set constraints: structured/validated output only, minimize data sent to the model, isolate the integration behind a swappable module boundary in `lib/ai/`, and keep cost/latency bounded and observable — see `roadmap.md`'s Phase 4a section. Not a decision to leave implicit inside implementation. **Resolved** — Google Gemini selected (`gemini-flash-lite-latest`/`gemini-flash-latest`), isolated entirely behind `src/lib/ai/client.ts` per the swappable-module constraint; 4a shipped and passed its review gate. | Solution Architect, AI Engineer |
| 16 | Splitting Phase 4 into 4a/4b/4c creates the same boundary-blur temptation Risk #13 flagged for 3a/3b (e.g., pulling Notifications v2's email delivery into 4a "since it's related to AI summaries," or an Admin screen into 4b) | 4a, 4b, 4c | Medium | Medium | The 4a/4b/4c boundaries defined in `roadmap.md` are binding, same as Risk #6/#13; CTO approval required to move any feature across a boundary. **Extended for 4b kickoff (2026-07-27):** the specific instance of this concern flagged at 4b's kickoff is the *reverse* direction — 4a's AI-generation code paths (`lib/ai/`) bleeding into 4b, rather than a whole feature moving across the boundary. See Risk #19, the concrete realized form of this general concern for the 4a/4b boundary specifically. | CTO |
| 17 | Notifications v2's email delivery (4b) is this codebase's first outbound email infrastructure and its first feature that sends financial content (balances, summaries) outside the app boundary entirely — a new data-egress and compliance surface (deliverability, unsubscribe/preference handling, PII in email body/subject) that the standing "no cross-user leakage" discipline (Risk #4) wasn't originally written with in mind | 4b | Medium | High | Solution Architect + Backend Engineer select and integrate the email provider within CTO-set constraints (see `roadmap.md`'s Phase 4b section); Security Architect review before release focused specifically on email content/PII exposure and unsubscribe/preference compliance, distinct from and in addition to the standard authorization review every feature gets. **Confirmed at 4b kickoff (2026-07-27):** unaffected by 4a's actual shape (4a shipped no email-adjacent infrastructure) — stands exactly as originally written. Any narrative text an email includes (e.g. a "monthly summary" notification) is bound by Risk #19's reuse-only constraint below, which further narrows this email-content review to PII/deliverability/compliance rather than also needing to cover freshly-generated AI prose. | Backend Engineer, Security Architect |
| 18 | Admin (4c) introduces this codebase's first privilege/authorization tier — no `role` or permission concept exists on `User` today — creating a new class of risk (privilege escalation / unauthorized access to other users' data via admin views) beyond the "wrong user, right role" model every prior phase's authorization checks were built around | 4c | Medium | High | Solution Architect + Database Architect decide the admin-authorization mechanism as its own explicit, reviewed milestone before any Admin backend work starts (same discipline Risk #9 required for 3a's Account-linkage decision — see `roadmap.md`'s Phase 4c section); Security Architect review is the headline focus of 4c's release gate, covering privilege escalation, audit-log tamper-resistance, and feature-flag access control specifically | Database Architect, Solution Architect, Security Architect |
| 19 | Reports' Monthly report and Notifications v2's "monthly summary" trigger (4b) could be implemented by independently regenerating narrative content via `lib/ai/`, instead of reusing 4a's already-generated, already-reviewed `MonthlySummary.narrative` verbatim — silently reintroducing an AI-generation/prompt-injection review surface into a phase this roadmap explicitly scoped as having none ("aggregation + rendering/delivery, not intelligence"), duplicating already-approved logic, and quietly expanding 4b's Security Architect review beyond what was budgeted (report/download authorization, email PII/deliverability) | 4b | Medium | Medium | CTO-set binding constraint at 4b kickoff (2026-07-27): Reports and Notifications v2 must not call `lib/ai/` directly, at all; any narrative content either surface includes is a verbatim, read-only reuse of the already-persisted `MonthlySummary.narrative` (the narrative section is simply omitted, never regenerated, when that row's `narrative` is null — the rest of the report/notification renders fully regardless) — see `roadmap.md`'s Phase 4b section. The report's own numeric/tabular data is independently sourced from existing Dashboard/Analytics/Debt/Investment/Budget services, never from `MonthlySummary.citedFigures` (too narrow to serve as a report's data source). Any genuine new AI-generation need discovered during the architecture pass is a scope question for the CTO, not a silent implementation decision. | Solution Architect, CTO |
| 20 | Notifications v2's "large purchase" and "low balance" triggers (4b) could be coupled to Spending Insights' (4a) AI-selected "notable merchant spend" logic instead of a simple deterministic threshold comparison, forcing a real-time, per-transaction/per-balance-check surface to depend on an LLM call it was never designed to make, and coupling two features with fundamentally different cost/latency contracts (periodic-refresh-and-cache vs. instant-fire) | 4b | Low | Medium | CTO-set binding constraint at 4b kickoff (2026-07-27): "large purchase" and "low balance" are deterministic numeric-threshold comparisons only, with zero `lib/ai/` involvement, fully independent of Spending Insights' code paths — some overlap in what a user sees between the two surfaces is expected and acceptable, not a defect to engineer around — see `roadmap.md`'s Phase 4b section | Solution Architect, Backend Engineer |

Update this register at the start of each phase's architecture review — add newly discovered risks, close ones that are no longer applicable, and do not delete rows (mark them resolved instead) so the history stays visible to the whole team.
