# FinanceOS — Risk Register

| # | Risk | Phase | Likelihood | Impact | Mitigation | Owner |
|---|------|-------|-----------|--------|------------|-------|
| 1 | Transaction/Account schema designed too narrowly in Phase 1, forcing breaking migrations when Debt/Investments (Phase 3a) need to reuse "account-like" concepts | 1→3a | High | High | Database Architect modeled `Account` with a `type` discriminator and shared balance/interest fields from the start. Resolved at 3a kickoff: Database Architect decided and documented the Debt/Investment linkage approach in `docs/database/` before 3a backend work began. **Resolved** — 3a shipped and passed its review gate. | Database Architect |
| 2 | AI transaction categorization / insights are vulnerable to prompt injection via user-controlled text (merchant names, transaction notes) | 4a | Medium | Medium | Treat all transaction text as untrusted input to the model; structured-output schema validation (Zod) on every AI response before it touches the database; Security Architect review required before release. **Extended for 4a kickoff (2026-07-21):** this is the first AI/LLM integration this codebase has ever had, so the mitigation now explicitly includes a *design-stage* Security Architect review (before backend implementation starts), in addition to the standing pre-release review — see `roadmap.md`'s Phase 4a milestones. **Resolved** — 4a shipped and passed its review gate (design-stage and pre-release Security Architect review both APPROVE — `docs/release/phase-4a-notes.md`). | AI Engineer, Security Architect |
| 3 | CSV import (Phase 1) is a common source of malformed data, encoding issues, and duplicate-transaction bugs | 1 | High | Medium | Backend Engineer validates and dedupes on import; Bug Hunter specifically stress-tests large/malformed CSVs before Phase 1 sign-off. **Resolved** — Phase 1 shipped and passed its review gate. | Backend Engineer, Bug Hunter |
| 4 | Financial data (balances, transactions) is high-sensitivity; any authorization bug exposes one user's finances to another | All | Low | Critical | Every API route/Server Action scoped by authenticated user ID at the query level (never trust client-supplied user/account IDs alone); Security Architect reviews every phase before release, not just at the end. **Extended for Phase 4b (2026-07-21):** this same discipline now explicitly covers PDF report generation/download (a user must never be able to generate or fetch another user's report) and outbound notification email content — see Risk #17. **Extended for Phase 4c:** now also covers admin-side data views (view users, audit logs) — see Risk #18. | Backend Engineer, Security Architect |
| 5 | PDF report generation (Phase 4) and large transaction tables (Phase 1+) risk becoming a performance/memory bottleneck as data grows | 1, 4b | Medium | Medium | Pagination and server-side filtering from day one in Phase 1 (not retrofitted); Performance Engineer reviews before each phase's release. This concern is concretely realized by Phase 3b's Analytics suite (see Risk #11) and again by Phase 4b's PDF generation. **Extended for 4b kickoff (2026-07-21):** the deployment target is serverless (Vercel, per Phase 0), which constrains PDF-library choice — headless-browser-based rendering carries cold-start, function-size, and execution-time limits in that environment that a non-browser JS-native renderer would not. Flagged as a binding constraint for the Solution Architect's 4b library choice, not just a post-hoc performance review item — see `roadmap.md`'s Phase 4b section. **Extended for 4b kickoff (2026-07-27):** 4a's cron routes (`monthly-summary`, `financial-health-score-snapshot`, the auto-categorization batch job) are now live, proven examples of scheduled background work succeeding on this same Vercel target — a concrete, already-working fallback shape (pre-generate reports on a schedule rather than render synchronously on request) if the Solution Architect's chosen renderer still can't meet Vercel's execution-time limit for on-demand generation. Context for that choice, not a new constraint — see `roadmap.md`'s Phase 4b section. | Performance Engineer, Solution Architect |
| 6 | Feature scope creep — roadmap is already large; pressure to pull later-phase features into an earlier phase for a "richer demo" | All | High | High | Roadmap phase boundaries are binding; CTO approval required to move any feature to an earlier phase or across the 3a/3b boundary (see Risk #13) or the 4a/4b/4c boundaries (see Risk #16) | CTO |
| 7 | Better Auth is a comparatively newer library vs NextAuth; ecosystem/adapter gaps could block Phase 0 | 0 | Low | Medium | Time-boxed Phase 0 spike; fall back to NextAuth if Better Auth blocks progress for more than 1 day. **Resolved** — Better Auth shipped cleanly in Phase 0, no fallback needed. | Backend Engineer |
| 8 | Timezone/date handling bugs in bills, budgets (monthly resets), and recurring schedules | 2 | Medium | Medium | Store all dates in UTC, compute "due today"/"month boundary" logic against the user's stored timezone preference, not server time; explicit Bug Hunter pass on this before Phase 2 sign-off. **Resolved** — Phase 2 shipped and passed its review gate. Note for Phase 4c: the Customization spec's "themes/currency display" work should not silently introduce a second timezone-preference source — Solution Architect should confirm the existing preference storage (from Phase 2) is reused, not duplicated. **Extended for Phase 4c kickoff (2026-07-29):** a schema grep at this kickoff found no timezone-related column on `User` (or anywhere else in `prisma/schema.prisma`) — this row's own claim of an existing "stored timezone preference" does not correspond to any field actually in the current schema. Either Phase 2 quietly simplified to server/UTC-only date-boundary logic without this row being corrected, or the field was never added. Customization's (4c) new per-user preferences model must therefore **newly add** this field as part of its own schema work, not merely reuse one, per `roadmap.md`'s Phase 4c CTO kickoff pass — flagged here so the Solution Architect isn't the first to discover this gap mid-implementation. **Extended for Phase 4c resolution pass (2026-07-29):** the CTO reviewed Customization's own draft AC2/AC4 (which committed to rewiring all nine already-shipped consumers' date-boundary logic plus four cron jobs, in this same phase) and made an explicit, reasoned scope call: 4c ships the timezone field itself only (selectable, persisted, browser-inferred default, UTC fallback) — the nine-consumer-plus-four-cron rewiring is descoped from 4c and deferred to a dedicated, later, explicitly-scoped pass, once a lower-regression-risk per-consumer migration strategy can be designed. `docs/product/customization.md`'s Timezone Preference capability was edited accordingly (its AC2/AC4 are retained, unedited, as target-state design, explicitly marked deferred rather than deleted). This gap is therefore **not closed** by 4c and remains genuinely open — see new Risk #29 below for the tracked deferred-work item itself, so it isn't lost between phases. | Backend Engineer, Bug Hunter |
| 9 | Debt/Investments schema shape (standalone models vs. extending the existing `Account` type discriminator, vs. a hybrid) decided hastily could force a breaking migration later once 3b's Net Worth aggregation and Analytics depend on it | 3a | Medium | High | Explicit Database Architect design decision as its own reviewed milestone before any Phase 3a backend work starts (not folded silently into implementation) — document the chosen approach and rationale in `docs/database/` before backend implementation begins. **Resolved** — decided and documented before 3a backend work began; 3a and 3b both shipped successfully on top of it. | Database Architect |
| 10 | Net Worth History chart (3b) has no historical data to chart if periodic snapshotting only begins when the chart UI itself is built | 3a→3b | High | Medium | Begin a lightweight periodic Net Worth Snapshot capture (backend only, no UI) at the end of Phase 3a — see `roadmap.md`'s Phase 3a milestones — so 3b's chart launches with real historical data already accumulated instead of an empty state. **Resolved** — shipped as scoped; the same "don't build the empty-history feature first" reasoning is reused for Phase 4c's Admin audit log (see Risk #16's split rationale in `roadmap.md`), and again for 4a's Financial Health Score snapshot job. | Backend Engineer |
| 11 | Analytics suite (3b) is compute-heavy over full Transaction/Account/Debt/Investment history (yearly spend, heatmaps, growth trends) — the general concern already flagged in Risk #5, now realized in concrete form | 3b | Medium | Medium | Performance Engineer reviews query/aggregation design before backend implementation begins (not only at phase-end release review); Solution Architect evaluates materialized/cached aggregate tables during the 3b architecture pass if live-query performance is a concern at realistic data volumes. **Resolved** — 3b shipped and passed its review gate. | Performance Engineer, Solution Architect |
| 12 | Financial Goals (broad, 3b) and Savings Goals (Phase 2) are conceptually adjacent — risk of user-facing duplication/confusion or two competing "goal" UIs if the boundary isn't explicit | 3b | Medium | Medium | Product Owner must define the explicit boundary and relationship between the two models in the 3b spec, before Database Architect schema work begins for `FinancialGoal`. **Resolved** — see `docs/product/financial-goals.md`'s explicit boundary decision; 3b shipped on top of it. | Product Owner |
| 13 | Splitting Phase 3 into 3a/3b creates a temptation to blur the boundary (e.g., pulling an Analytics metric into 3a "since it's related," or a Debt/Investment field into 3b) | 3a, 3b | Medium | Medium | The 3a/3b boundary defined in `roadmap.md` is binding, same as Risk #6; CTO approval required to move any feature across it. **Resolved** — no cross-boundary moves occurred; v1 shipped with the boundary intact. | CTO |
| 14 | Financial Health Score (Phase 4a) scope is ambiguous — an LLM-computed judgment vs. a deterministic formula over existing Analytics/Debt/Budget data with an optional AI-generated narrative — and guessing wrong misassigns module ownership (AI Engineer vs. Backend Engineer) before schema/architecture work | 4a | Medium | Medium | Product Owner must resolve this explicitly in the 4a spec, before the Solution Architect + AI Engineer architecture pass — see `roadmap.md`'s Phase 4a open scope question, following the same discipline Risk #12 established for the SavingsGoal/FinancialGoal boundary. **Resolved** — `docs/product/ai-features.md` resolved this as a deterministic formula with an optional AI narrative layer, CTO-approved weighting/bands (2026-07-22); 4a shipped and passed its review gate on top of it. | Product Owner |
| 15 | LLM/AI provider selection (Phase 4a) carries data-privacy (transaction/merchant text sent to a third party), cost, latency, and vendor-lock-in dimensions — this codebase has no existing AI/LLM integration to build on, so there is no established pattern to reuse or fall back to | 4a | Medium | High | Solution Architect + AI Engineer make this decision jointly during 4a's architecture pass, bound by CTO-set constraints: structured/validated output only, minimize data sent to the model, isolate the integration behind a swappable module boundary in `lib/ai/`, and keep cost/latency bounded and observable — see `roadmap.md`'s Phase 4a section. Not a decision to leave implicit inside implementation. **Resolved** — Google Gemini selected (`gemini-flash-lite-latest`/`gemini-flash-latest`), isolated entirely behind `src/lib/ai/client.ts` per the swappable-module constraint; 4a shipped and passed its review gate. | Solution Architect, AI Engineer |
| 16 | Splitting Phase 4 into 4a/4b/4c creates the same boundary-blur temptation Risk #13 flagged for 3a/3b (e.g., pulling Notifications v2's email delivery into 4a "since it's related to AI summaries," or an Admin screen into 4b) | 4a, 4b, 4c | Medium | Medium | The 4a/4b/4c boundaries defined in `roadmap.md` are binding, same as Risk #6/#13; CTO approval required to move any feature across a boundary. **Extended for 4b kickoff (2026-07-27):** the specific instance of this concern flagged at 4b's kickoff is the *reverse* direction — 4a's AI-generation code paths (`lib/ai/`) bleeding into 4b, rather than a whole feature moving across the boundary. See Risk #19, the concrete realized form of this general concern for the 4a/4b boundary specifically. | CTO |
| 17 | Notifications v2's email delivery (4b) is this codebase's first outbound email infrastructure and its first feature that sends financial content (balances, summaries) outside the app boundary entirely — a new data-egress and compliance surface (deliverability, unsubscribe/preference handling, PII in email body/subject) that the standing "no cross-user leakage" discipline (Risk #4) wasn't originally written with in mind | 4b | Medium | High | Solution Architect + Backend Engineer select and integrate the email provider within CTO-set constraints (see `roadmap.md`'s Phase 4b section); Security Architect review before release focused specifically on email content/PII exposure and unsubscribe/preference compliance, distinct from and in addition to the standard authorization review every feature gets. **Confirmed at 4b kickoff (2026-07-27):** unaffected by 4a's actual shape (4a shipped no email-adjacent infrastructure) — stands exactly as originally written. Any narrative text an email includes (e.g. a "monthly summary" notification) is bound by Risk #19's reuse-only constraint below, which further narrows this email-content review to PII/deliverability/compliance rather than also needing to cover freshly-generated AI prose. | Backend Engineer, Security Architect |
| 18 | Admin (4c) introduces this codebase's first privilege/authorization tier — no `role` or permission concept exists on `User` today — creating a new class of risk (privilege escalation / unauthorized access to other users' data via admin views) beyond the "wrong user, right role" model every prior phase's authorization checks were built around | 4c | Medium | High | Solution Architect + Database Architect decide the admin-authorization mechanism as its own explicit, reviewed milestone before any Admin backend work starts (same discipline Risk #9 required for 3a's Account-linkage decision — see `roadmap.md`'s Phase 4c section); Security Architect review is the headline focus of 4c's release gate, covering privilege escalation, audit-log tamper-resistance, and feature-flag access control specifically. **Extended for Phase 4c kickoff (2026-07-29):** the CTO kickoff pass resolved the *scope* this mechanism must serve — a single flat privilege tier (no per-resource RBAC, no self-service role assignment), reflecting this app's actual shape (many independent end users, no organizations, a small internal operating team) — see Risk #27 below for the tracked consequence of that scope call, and `roadmap.md`'s Phase 4c kickoff pass for the full reasoning. The mechanism itself (role field vs. permissions table vs. Better Auth's own `admin` plugin) remains this milestone's open decision. | Database Architect, Solution Architect, Security Architect |
| 19 | Reports' Monthly report and Notifications v2's "monthly summary" trigger (4b) could be implemented by independently regenerating narrative content via `lib/ai/`, instead of reusing 4a's already-generated, already-reviewed `MonthlySummary.narrative` verbatim — silently reintroducing an AI-generation/prompt-injection review surface into a phase this roadmap explicitly scoped as having none ("aggregation + rendering/delivery, not intelligence"), duplicating already-approved logic, and quietly expanding 4b's Security Architect review beyond what was budgeted (report/download authorization, email PII/deliverability) | 4b | Medium | Medium | CTO-set binding constraint at 4b kickoff (2026-07-27): Reports and Notifications v2 must not call `lib/ai/` directly, at all; any narrative content either surface includes is a verbatim, read-only reuse of the already-persisted `MonthlySummary.narrative` (the narrative section is simply omitted, never regenerated, when that row's `narrative` is null — the rest of the report/notification renders fully regardless) — see `roadmap.md`'s Phase 4b section. The report's own numeric/tabular data is independently sourced from existing Dashboard/Analytics/Debt/Investment/Budget services, never from `MonthlySummary.citedFigures` (too narrow to serve as a report's data source). Any genuine new AI-generation need discovered during the architecture pass is a scope question for the CTO, not a silent implementation decision. | Solution Architect, CTO |
| 20 | Notifications v2's "large purchase" and "low balance" triggers (4b) could be coupled to Spending Insights' (4a) AI-selected "notable merchant spend" logic instead of a simple deterministic threshold comparison, forcing a real-time, per-transaction/per-balance-check surface to depend on an LLM call it was never designed to make, and coupling two features with fundamentally different cost/latency contracts (periodic-refresh-and-cache vs. instant-fire) | 4b | Low | Medium | CTO-set binding constraint at 4b kickoff (2026-07-27): "large purchase" and "low balance" are deterministic numeric-threshold comparisons only, with zero `lib/ai/` involvement, fully independent of Spending Insights' code paths — some overlap in what a user sees between the two surfaces is expected and acceptable, not a defect to engineer around — see `roadmap.md`'s Phase 4b section | Solution Architect, Backend Engineer |
| 21 | Notifications v2's new `evaluate-notifications` cron route (4b) is this codebase's first batch job whose per-iteration failure mode is externally visible and irreversible — a misdirected or cross-user-mixed email cannot be recalled the way a bad database write can be corrected, unlike every prior cron (`net-worth-snapshot`, `categorize-transactions`, `monthly-summary`, `financial-health-score-snapshot`), which only ever wrote to that same user's own rows | 4b | Low | High | Structural mitigation specified in `docs/architecture/phase-4b-technical-design.md` §5/§8: every trigger's evaluation constructs exactly one data object per user per event, reused for both the in-app `Notification` row and the email template's props — no second, independently-scoped read that could resolve to a different user; no batch/merge-variable send API is ever used (one `sendNotificationEmail` call per user per event, never a provider call handling multiple recipients' content at once). Flagged for verification at the Security Architect's 4b review gate, not fully closed out by design alone. | Solution Architect, Backend Engineer, Security Architect |
| 22 | Reports' custom start/end date-range extension (4b, `features/reports/`, needed for Income/Expense/Cash Flow Reports) has no natural upper bound the way Analytics' four existing presets do — an adversarial or malformed request could trigger an unbounded aggregation query | 4b | Low | Medium | `docs/architecture/phase-4b-technical-design.md` §3: `GenerateReportRequestSchema` must enforce an explicit maximum range length (e.g. clamped to a number of years, or to the user's own account-creation date) — a Backend Engineer implementation requirement, not optional | Backend Engineer |
| 23 | `@react-pdf/renderer`'s automatic pagination (4b, the PDF library chosen for Reports) still requires deliberate per-table `wrap`/overflow configuration — a misconfigured table could silently truncate a long list (Largest Purchases, Top Merchants, a full year's monthly trend rows) rather than erroring, producing a PDF that looks complete but isn't, violating reports.md's own Cross-Cutting Requirement #6 ("a generation failure is honest and recoverable... never a silently corrupted file") | 4b | Medium | Medium | `docs/architecture/phase-4b-technical-design.md` §2/§8: every `<ReportTable>`-composing template must be tested against a high-row-count fixture account as part of this feature's own test suite; flagged explicitly for the Bug Hunter/Performance Engineer's review at the 4b gate | Backend Engineer, Bug Hunter, Performance Engineer |
| 24 | Notifications v2's GOAL_ACHIEVED trigger (4b) shipped without ever closing a self-identified gap in its own required data migration: `prisma/migrations/20260728082118_phase_4b_reports_notifications_v2/migration.sql` backfilled `FinancialGoal.completionNotifiedAt` (the "no retroactive fire for already-complete goals" latch, notifications-v2.md's own binding Goal Achieved edge case) for `DEBT_PAYOFF` and `NET_WORTH_SAVINGS_TARGET` goals, but explicitly deferred `SAVINGS_RATE_TARGET` goals — no rolling-savings-rate formula existed anywhere in this codebase yet to replicate in raw SQL at the time — and asked the Backend Engineer/Solution Architect to either re-run an equivalent backfill once that formula existed or explicitly accept the gap. Neither happened before `goal-achieved-trigger.ts` shipped, caught only at the Phase 4b release gate (`docs/release/phase-4b-notes.md` Section 1, REJECT). | 4b | Low (narrow precondition: only affects a user with an already-complete `SAVINGS_RATE_TARGET` goal at the moment the original migration ran) | Medium (bounded to exactly one incorrect "goal achieved" notification per affected goal, per the `@@unique([financialGoalId, type])` constraint — not a flood, not a data-integrity issue, but a real, confusing, spec-contradicting user-facing message) | **Closed.** `prisma/backfill-savings-rate-goal-notifications.ts` (`npm run backfill:savings-rate-completion`) is a one-time, idempotent TypeScript backfill that reuses the actual, now-genuinely-available `computeCurrentRollingSavingsRatePercent`/`getFinancialGoalCompletionStatus` logic (`src/features/financial-goals/server/service.ts`) rather than re-deriving that formula in raw SQL a second time. The already-applied migration's SQL was left untouched (never edit an applied migration); only its comment was updated to point at this script instead of still describing the gap as open. **Operational note, binding for deploy:** this script does not run automatically — it must be run once against each environment, including production, before/during the Phase 4b deploy, or the gap remains open for any pre-existing affected goal in that environment. See `docs/release/phase-4b-checklist.md` for where this step belongs in the deploy sequence. | Backend Engineer |
| 25 | Admin's "manage categories" capability (4c) cannot function as currently built — the global starter-category template (`DEFAULT_CATEGORIES`, `src/features/categories/default-categories.ts`) is a hardcoded TypeScript constant read directly by `src/lib/auth.ts`'s signup hook, with no corresponding database row/table for an admin screen to edit at runtime | 4c | High (near-certain given the current implementation) | Medium | Flagged at CTO kickoff (2026-07-29): Database Architect must add a DB-backed system-category-template model as part of 4c's Solution Architect + Database Architect pass, and `lib/auth.ts`'s signup hook must be migrated to read from it instead of the static array, before Admin's manage-categories capability is implemented — see `roadmap.md`'s Phase 4c kickoff pass | Database Architect, Backend Engineer |
| 26 | "Seed demo data" (Admin, 4c), if implemented without an explicit target/environment scope, could overwrite or pollute a real user's financial data, or become an unbounded data-generation vector reachable in production | 4c | Low | High | Flagged at CTO kickoff (2026-07-29): Product Owner's Admin spec must explicitly scope this capability (a dedicated demo user and/or a non-production-only restriction) before backend implementation begins — see `roadmap.md`'s Phase 4c kickoff pass. **Resolved by the Product Owner's spec** (`docs/product/admin.md`, Capability 6): both guardrails together — fixed `showcase@lkbudget.demo` target only, non-production environments only — confirmed as a correct, sufficient resolution by the Phase 4c CTO resolution pass (2026-07-29). | Product Owner, Backend Engineer, Security Architect |
| 27 | Admin's authorization model (4c) is deliberately scoped at CTO kickoff to a single flat privilege tier (no per-resource RBAC, no tiered admin roles, no self-service role-assignment UI) — the right-sized call for this app's actual shape today (many independent end users, no organizations, a small internal operating team), but a call that will need revisiting if a future need for finer-grained admin access emerges | 4c | Low | Low | No mitigation needed today — tracked so this scope call isn't silently forgotten if requirements change later; see `roadmap.md`'s Phase 4c kickoff pass | CTO |
| 28 | Customization's (4c) "dashboard layout/widgets" and "currency display," read literally, are broad enough to be mistaken for a generic drag-and-drop widget-builder framework and/or genuine multi-currency data support — neither is warranted at this app's scale, and `prisma/schema.prisma` has no currency field anywhere to convert from | 4c | Medium | Medium | Flagged and scoped at CTO kickoff (2026-07-29): dashboard customization is show/hide + reorder of the existing fixed card set only; currency display is a display-formatting preference over already-USD-denominated amounts only. Product Owner's Customization spec must reflect this scope explicitly. **Confirmed by the Phase 4c CTO resolution pass (2026-07-29):** `docs/product/customization.md` reflects both boundaries correctly as written — no spec edit needed for these two capabilities. | Product Owner, Solution Architect |
| 29 | Deferred by the Phase 4c CTO resolution pass (2026-07-29): Bills, Budgeting, Recurring Income, Calendar v2, Dashboard, Analytics, Automatic Monthly Summaries, Notifications v2, Financial Goals, and Reports (nine already-shipped features) plus four cron jobs (Monthly Summary, Net Worth Snapshot, Financial Health Score Snapshot, `evaluate-notifications`) all continue computing date/month boundaries against server/UTC time, not the new per-user timezone field Customization (4c) adds — a deliberate, explicit scope-down from Customization's own original draft AC2/AC4, not an oversight or a silently dropped requirement | 4c→future (unscheduled) | High (near-certain to be wrong for any user meaningfully outside the deploy server's own timezone, at the exact day/month boundary) | Medium (a day-boundary-adjacent correctness bug consistent with the product's status quo since Phase 2 — not a new regression, not a data-integrity or security issue, and bounded to the boundary moment itself, not the whole day) | Not mitigated in 4c, by explicit CTO scope call — see Risk #8's extension and `roadmap.md`'s Phase 4c resolution pass for the full reasoning (rewiring nine already-shipped, already-reviewed consumers' date logic plus four cron jobs is a materially different, higher-regression-risk undertaking than this phase's actual new capabilities, and belongs in its own dedicated, later, explicitly-scoped pass). `docs/product/customization.md`'s Timezone Preference AC2/AC4 are retained as the target-state design for that future pass. This row exists so the deferred work itself has a durable home in the register and gets picked up deliberately, not forgotten. | CTO, Solution Architect, Backend Engineer |
| 30 | Reports (4b) has no persisted generation-event row — `generateReport` (`src/features/reports/server/service.ts`) and its Route Handler (`src/app/api/reports/route.ts`) return a PDF buffer directly with no database write of any kind — so Admin's Audit Log capability (4c, Capability 3 AC1) has no existing row to surface for "each PDF report generated," unlike the other three audited event types (category suggestions, notification email status, and — pending the architecture pass — feature-flag/template-change history) | 4c | High (confirmed — no such model exists anywhere in `prisma/schema.prisma`, and `service.ts`/`route.ts` were both read directly to confirm) | Low (a missing operational-visibility row, not a data-integrity or security defect — report generation itself functions correctly today) | Confirmed genuine by the Phase 4c CTO resolution pass (2026-07-29), by direct inspection of `src/features/reports/server/service.ts`, `src/app/api/reports/route.ts`, and `prisma/schema.prisma`. Routed to the Solution Architect + Database Architect's 4c combined schema pass (`roadmap.md` milestone 3) as a fifth schema question alongside the admin-authorization mechanism, Calendar v2's (zero-schema) composition, the Customization preferences model, and the system-category-template model: add a lightweight report-generation-event log and have `generateReport`/the reports Route Handler write to it. | Database Architect, Backend Engineer |
| 31 | Admin's Audit Log (4c) has no retention/expiration policy defined anywhere — indefinite growth of category-suggestion, report-generation, and notification-email audit rows is a real long-term storage question, flagged as an open question in `docs/product/admin.md` | 4c | Low (near-term; grows slowly over time) | Low | Decided by the Phase 4c CTO resolution pass (2026-07-29): indefinite retention for this phase — no automatic deletion/archival is built now. The audited event types are a small, bounded subset of total app activity (AI/report/notification/admin-action events only, not a blanket audit-everything log per `admin.md` Capability 3 AC5), so building expiration/archival infrastructure ahead of any demonstrated storage-cost problem would be the same kind of speculative infrastructure this phase's kickoff pass already rejected for dashboard widgets and multi-currency (Risk #28). Revisit if/when a real storage-volume signal emerges. Tracked here so the "forever, by default" decision is explicit, not silently assumed. | CTO, Database Architect |
| 32 | Admin's Feature Flags capability (4c) defines two initial flags (AI features, email delivery) but no standing process for when a future feature should register its own flag, flagged as an open question in `docs/product/admin.md` | 4c→future | Low | Low | Decided by the Phase 4c CTO resolution pass (2026-07-29): no formal flag-registry process is built now. Instead, a lightweight standing rule — every future phase's own CTO kickoff pass must explicitly evaluate whether that phase introduces a new high-risk automated/data-egress surface (AI-generated content, third-party egress, a background job touching many users' data) and, if so, decide at that time whether it warrants its own feature-flag key, registered against the standalone feature-flag primitive the Solution Architect's 4c architecture pass is already asked to consider (per `roadmap.md`'s Phase 4c kickoff recommendation) — the same case-by-case discipline already used to decide each phase's Security Architect review scope, not a blanket "every feature gets a flag" mandate. Tracked here so this process isn't reinvented ad hoc each time. | CTO |
| 33 | Admin's cross-user reads (`features/admin/server/users.ts`, `audit-log.ts`) and Reports' new `getReportGenerationEvents` (`features/reports/server/audit.ts`) are this codebase's first-ever query functions not scoped to a single authenticated user's own ID — a deliberate, narrow exception to Risk #4's standing rule | 4c | Low | High | Solution Architect's 4c pass (`phase-4c-technical-design.md` §7/§9): every such function is called exclusively from behind the new `getCurrentAdminUser()` helper, never from any ordinary session-authenticated path. Flagged as the headline concern, alongside admin-authorization privilege escalation itself, for the Security Architect's 4c review gate — verification that this exception is never reachable outside `app/admin/` is required before release, not assumed from design alone. | Backend Engineer, Security Architect |
| 34 | `lib/feature-flags.ts`'s `isFeatureEnabled` check (4c) sits directly on the AI/email hot path — read inside `lib/ai/generate-structured-output.ts` and `lib/email/send-notification-email.ts`'s own single existing choke points — so a transient database read failure on this one small table could, if implemented naively, spuriously disable AI/email app-wide | 4c | Low | Medium | `phase-4c-technical-design.md` §6.1: `isFeatureEnabled` must fail OPEN (resolve to `true`, features stay enabled) on any read error or a missing row, never fail closed — an explicit implementation requirement, not left to be inferred. A short in-process TTL cache is recommended (non-binding) to keep this addition from adding meaningful latency to either hot path. | Backend Engineer |
| 35 | `SystemCategoryTemplate` (4c) is this schema's first genuinely global, non-per-user table, requiring a one-time, ordered deploy-time seed from today's `DEFAULT_CATEGORIES` constant before `lib/auth.ts`'s signup hook is switched over to read from it | 4c | Low (one-time, well-understood deploy step) | Medium (getting the order wrong would change new-signup behavior on day one, the exact regression AC7's non-retroactivity guarantee is meant to prevent) | `phase-4c-technical-design.md` §4.3: seed the eleven `SystemCategoryTemplate` rows, in `DEFAULT_CATEGORIES`' current array order, BEFORE deploying the signup-hook code change — the same ordered-deploy-step discipline Phase 4b's `FinancialGoal.completionNotifiedAt` backfill already established. Tracked in `docs/release/`'s eventual Phase 4c deploy checklist. | Database Architect, Backend Engineer |
| 36 | Several Phase 4c fields (`DashboardCardPreference.cardKey`, `FeatureFlag.key`, `UserPreference.accentColor`/`currencyDisplay`) are deliberately plain, application-validated `String` columns rather than DB enums, for forward-compatibility reasons — a stale or renamed key (e.g. a Dashboard card removed in a later phase while old preference rows still reference it) could crash a render if not handled defensively | 4c | Low | Low | `phase-4c-technical-design.md` §3.4/§3.5/§6.1: every materialization function that merges a stored key against its current canonical list must ignore an unrecognized key gracefully (skip it, never throw) — an explicit implementation requirement for whoever writes `getDashboardCardPreferences`, `getFeatureFlags`, and the accent-color/currency-display read paths. | Backend Engineer |
| 37 | `AdminActionLog` (4c) is a genuinely new persistence need this pass identified — a load-bearing prerequisite for Admin's own Feature Flags/Manage Categories/Seed Demo Data capabilities to satisfy their own "this action is worth recording" requirement (`admin.md` Capabilities 4/5/6) — but it is not one of the CTO's five enumerated schema questions and could otherwise be missed until Admin's own Definition of Done is checked against a shipped implementation | 4c | Low (caught at this architecture pass, not discovered later) | Low | Designed in full in `phase-4c-technical-design.md` §6.2, in the same combined Solution Architect + Database Architect pass as the five enumerated schema questions — not an afterthought once backend implementation is already underway. | Database Architect, Backend Engineer |
| 38 | Better Auth's official `admin` plugin (already an installed dependency of the base `better-auth` package, per `package.json`) is evaluated and explicitly rejected as this phase's authorization mechanism (`phase-4c-technical-design.md` §1.2) in favor of a plain `role` column — a future engineer skimming `package.json` alone could mistakenly assume the plugin is active and reachable | 4c | Low | Low | Documented explicitly in `phase-4c-technical-design.md` §1.2/§10: the plugin is never enabled via `plugins: [...]` in `lib/auth.ts`, and no code path in this codebase ever calls `betterAuth({ plugins: [admin()] })`. Flagged here so this remains a documented, deliberate non-use, not a silent gap a future dependency-audit tool might flag as "installed but unused" without context. | CTO, Solution Architect |
| 39 | Phase 5's Motion & Craft workstream (5b), if built on screens whose structure the Accessibility/Responsive workstream (5a) later restructures (a new bottom-nav component, disclosure-widget semantics for expandable cards, mobile card-list conversions of data tables), would need significant animation rework redone once that structure changes — wasted work if sequencing isn't enforced | 5 | Medium | Medium | CTO kickoff pass (2026-07-30) split Phase 5 into 5a (Accessibility & Responsive Foundation) and 5b (Motion & Craft), with a binding phase-gate rule identical to every prior split in this project (5b's architecture does not start until 5a's review gate passes) — see `roadmap.md`'s Phase 5 CTO kickoff pass, Section 1 | CTO |
| 40 | `prefers-reduced-motion` has no handling anywhere in this codebase today — confirmed by direct grep of `src/` and `globals.css`, zero matches — even though Framer Motion (this app's designated animation library per `ui-component-engineer.md`'s own charter) is already a live dependency, used today in `components/shared/progress-ring.tsx`. Phase 5b's planned expansion of motion across every screen would multiply this pre-existing, previously-undetected gap rather than introduce a new one, if not addressed as its own explicit requirement | 5b | Medium | Medium | CTO-set binding constraint at Phase 5 kickoff (2026-07-30): every animation Phase 5b adds must respect `prefers-reduced-motion` via one shared, central mechanism (e.g. `MotionConfig reducedMotion="user"` at the app shell) — not a per-component ad hoc check — and this is a required acceptance criterion, not optional polish. See `roadmap.md`'s Phase 5 CTO kickoff pass, Section 3 | Solution Architect, UI Component Engineer |
| 41 | The E2E Test Engineer role (`.claude/agents/e2e-test-engineer.md`) — whose own charter already names "Accessibility" as a required Playwright test target, and whom Phase 5's own roadmap stub already names as the accessibility workstream's reviewer — has never been dispatched at any point in this project: no `tests/e2e/` directory exists, no `docs/testing/e2e/` reports exist, and `playwright`/`@playwright/test` is not a dependency in `package.json`, all confirmed by direct inspection at Phase 5 kickoff | 5a | High (confirmed — near-certain to block 5a's gate if not addressed first, since no automated a11y coverage of any kind exists to build on) | Medium | Flagged at Phase 5 kickoff (2026-07-30): bootstrapping Playwright plus an accessibility-testing addon (e.g. `@axe-core/playwright`) is itself a prerequisite task for 5a's Solution Architect pass, not an afterthought inside "invoke the E2E Engineer" — see `roadmap.md`'s Phase 5 CTO kickoff pass, milestone list for 5a | Solution Architect, E2E Test Engineer |
| 42 | Phase 5's "bottom navigation on mobile" (5a) risks becoming a second, competing mobile-navigation mechanism alongside the already-shipped, working hamburger-`Sheet` mobile sidebar (`components/shared/top-nav.tsx`/`sidebar.tsx`'s `mobile` prop, live since Phase 0) if its relationship to that existing mechanism isn't explicitly resolved before implementation — a direct "avoid duplication" violation if left ambiguous | 5a | Medium | Medium | Flagged at Phase 5 kickoff (2026-07-30): Product Owner's 5a spec must explicitly state whether bottom nav replaces the existing mobile Sheet-sidebar or serves a distinct, narrower role (e.g. the 4–5 most-used routes) alongside it — not left ambiguous, the same class of stale-scope check 4c's kickoff pass applied to "custom categories" | Product Owner, Frontend Lead |
| 43 | Customization's (4c) per-user accent-color presets crossed with light/dark theme meaningfully multiply the color-contrast surface an accessibility audit (5a) must cover — a combination that did not exist before 4c shipped (this app had exactly one default light/dark theme with no accent choice through Phase 4b) | 5a | Medium | Medium | Flagged at Phase 5 kickoff (2026-07-30): 5a's contrast-audit acceptance criteria must explicitly enumerate every accent-color preset × light/dark theme combination, not just the default theme — see `roadmap.md`'s Phase 5 CTO kickoff pass, Section 3 | E2E Test Engineer, UI Component Engineer |
| 44 | Framer Motion is an already-installed, already-approved dependency (per `ui-component-engineer.md`'s own charter), but is used in exactly one component today (`components/shared/progress-ring.tsx`) — Phase 5b's "dedicated pass across all existing screens" is a real, non-trivial increase in that library's footprint across the client bundle and hydration cost, not a marginal addition already paid for | 5b | Medium | Medium | Performance Engineer's 5b gate review must measure actual bundle-size delta and interaction-latency impact from the wider rollout specifically, per `roadmap.md`'s Phase 5 CTO kickoff pass acceptance criteria (GPU-compositable properties preferred, 60fps target, no TTI regression) | Performance Engineer |
| 45 | No visual-regression or cross-breakpoint/cross-theme screenshot testing exists anywhere in this codebase today — a systematic responsive audit (5a) and a systematic motion pass (5b) are both sweeping, every-screen changes that a manual, ad hoc "checked the one breakpoint/state I thought to check" review is prone to under-verify | 5a, 5b | Low | Low | Not mandated by this pass — flagged for the Solution Architect's 5a architecture pass to weigh (adopt a visual-regression tool now vs. rely on the E2E Test Engineer's/Bug Hunter's manual multi-breakpoint/multi-theme coverage), the same "flagged, not decided here" treatment 4b's kickoff gave the PDF-library-vs-cron-fallback tradeoff | Solution Architect, CTO |

Update this register at the start of each phase's architecture review — add newly discovered risks, close ones that are no longer applicable, and do not delete rows (mark them resolved instead) so the history stays visible to the whole team.
