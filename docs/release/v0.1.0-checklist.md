# v0.1.0 Deployment / Phase-Gate Checklist — Phase 3b (v1 sign-off)

Status legend: [x] verified pass · [~] pass with a documented, accepted follow-up (not blocking)

Note: this checklist went through two passes. The first pass found one real,
undisclosed Definition-of-Done gap (Analytics test coverage — see the item
below, and `v0.1.0-notes.md` for full detail) and returned BLOCK. That gap
was remediated in commit `8384c79` and independently re-verified (diff
review confirming mechanical, no-formula-change extractions, plus direct
inspection of the new tests and a from-scratch re-run of typecheck/lint/
test/build). Every item below reflects the current, post-remediation state.

## Acceptance criteria / Definition of Done

- [x] Net Worth History chart — all ACs and DoD items verified against
      `net-worth-history.md` (range selector, defaults, sparse-history state,
      breakdown toggle, hover, legibility/thinning, "as of" labeling, gaps
      handled without fabrication, negative net worth). Range-boundary math
      (`resolveRangeStart`) and thinning (`thinRows`) are unit-tested.
- [x] Financial Goals — all three goal types' CRUD, progress/completion math,
      and the Debt Payoff one-active-goal-per-Debt exclusivity rule verified,
      including at the previously-broken archive→create→unarchive sequence
      (fix confirmed present and correct in `actions.ts`). Progress math for
      all three types is fixture-tested (`progress-math.test.ts`).
- [~] Financial Goals — Savings Rate Target's past-`targetDate` "overdue"
      state gap: real, but correctly filed as low-severity/needs-Product-
      Owner-UX-decision, not a functional defect. Accepted, documented
      follow-up.
- [x] **Analytics — test coverage requirement met (remediated).**
      `analytics.md`'s DoD requires fixture tests for Yearly Spending,
      Category Trends, Budget vs. Actual, Income Growth, Income Sources, and
      Savings Growth's month/year-boundary aggregation, plus Savings
      Growth's gain/loss-adjustment fixtures. Commit `8384c79` extracted the
      pure math from each of the four flagged files
      (`buildYearlySpendingPoints`/`buildCategoryTrends`,
      `reshapeBudgetMonthView`, `buildIncomeGrowthPoint`,
      `computeSavingsGrowthPoint`) and added `period.test.ts` (24),
      `spending-trends.test.ts` (15), `budget-comparison.test.ts` (7),
      `income-analytics.test.ts` (12), `savings-growth.test.ts` (8) — 66
      tests. Diff-reviewed all four extractions line by line: mechanical,
      no formula changes. Read all five new test files: substantive
      coverage of real boundary cases (leap-year February, Dec 31 → Jan 1
      crossings, January-anchored `LAST_12_MONTHS` rollover, negative-zero
      normalization, the DoD-named $0-holdings/gain-vs-loss Savings Growth
      scenarios), not smoke tests.
- [x] Analytics — Subscription Cost Detection heuristic (3-occurrence
      minimum, price-change continuation, Active/Possibly-Cancelled status)
      is fixture-tested and passes.
- [x] Analytics — duplicate-computation performance fix confirmed real and
      load-bearing: `deriveIncomeSourcesFromGrowth` /
      `deriveActiveSubscriptionAnnualizedTotal` are defined and are the
      functions actually called from `analytics/page.tsx`, not dead code
      alongside the still-redundant standalone versions.
- [~] Analytics — `getBudgetVsActual`/`getSavingsGrowth`'s N-queries-per-
      month-in-range pattern: documented, reasoned deferral, doesn't affect
      correctness, acceptable at current expected scale. Accepted follow-up.

## Security

- [x] Security Architect review read in full (`docs/security/phase-3b-review.md`)
      — PASS, no High/Critical findings.
- [x] Spot-checked cited findings against current source (Financial Goals
      cross-user Account/Debt id rejection, subscription dismissal upsert
      scoping by compound key including `userId`) — hold up under direct
      re-reading.
- [~] No app-level rate limiting on mutating Server Actions — pre-existing,
      whole-codebase condition, not a Phase 3b regression, correctly
      deferred to a general backlog item, not this phase's blocker.

## Performance

- [x] Duplicate-computation fix (Income Growth/Sources,
      Subscriptions/annualized-total) confirmed implemented and wired in.
- [~] `getBudgetVsActual`/`getSavingsGrowth` N-queries-per-month-in-range
      scaling characteristic — documented, deliberately deferred, doesn't
      produce incorrect data or a hard failure at current expected scale.

## Bug Hunter

- [x] Debt Payoff exclusivity bypass via unarchive — fixed, fix verified
      present and correctly transaction-scoped.
- [x] Subscription dismissal permanence/invisibility — fixed, fix verified
      present (`getDismissedSubscriptionMerchants`/
      `undismissSubscriptionMerchant`) and wired into the Analytics page UI.
- [~] Savings Rate Target goal past-`targetDate` no "overdue" state — real,
      low severity, correctly deferred pending a Product Owner UX decision.

## Build / tooling (re-run independently, twice — before and after remediation)

- [x] `npm run typecheck` — clean, 0 errors (both passes).
- [x] `npm run lint` — clean, 0 errors/warnings (both passes).
- [x] `npm run test` — 87/87 (first pass) → **153/153 tests passing across
      12 suites** (post-remediation, matching the claimed 87 prior + 66
      new, independently re-run from scratch).
- [x] `npm run build` — production build succeeds, all 27 routes generated,
      identical route list both passes (no regression from the refactor).
- [x] `npx prisma migrate status` — all 5 migrations applied to the actual
      database; schema in sync.
- [x] No stray temporary diagnostics or `console.log` left in `src/` from
      earlier production-hotfix commits (`2d7c735`'s diagnostic confirmed
      removed from `next.config.ts`).

## Cross-phase consistency (v1 sign-off sanity pass, not just this phase)

- [x] `src/features/dashboard/server/service.ts` (Phase 1, extended in
      3a) — `getNetWorth` correctly folds in 3a's debt liability exactly
      once and requires zero Investments-specific change (holdings write
      back to `Account.balance`); Phase 1's monthly-trend/summary logic is
      undisturbed.
- [x] `src/components/ui/dialog.tsx` (shared primitive, touched once in
      3a) — no inconsistency found; used uniformly across features.
- [x] Phase 3a's own gate (`docs/release/phase-3a-checklist.md`/
      `phase-3a-notes.md`) already passed in a prior session; nothing in
      this pass's review of Phase 3b's changes indicates a 3a regression.

## Documentation

- [x] All three Phase 3b product specs, the security review, and all three
      bug reports exist, are complete, and are internally consistent with
      the shipped code.
- [x] The Analytics test-coverage gap this gate found is now documented in
      full in `v0.1.0-notes.md` (both the original finding and the
      remediation), closing the earlier disclosure gap.

## Overall Gate Decision

**PASS. v1 is ready to ship**, per `project-charter.md`'s Success
Definition. See `v0.1.0-notes.md` for the full two-pass verification
history and justification.
